---
phase: 13-pdf-first-workspace-shell
plan: 05
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - frontend/components/workspace-v2/left-detail-panel.tsx
  - frontend/components/workspace-v2/left-panel-toggle.tsx
  - frontend/components/workspace-v2/right-rail-stats.tsx
  - frontend/components/workspace-v2/workspace-skeleton.tsx
  - frontend/components/workspace-v2/shell.tsx
  - frontend/app/workspace-v2/[job_id]/page.tsx
  - frontend/app/workspace-v2/[job_id]/loading.tsx
  - frontend/lib/workspace.ts
autonomous: true
requirements:
  - LAYOUT-01
  - PDF-02
  - PDF-03

must_haves:
  truths:
    - "LeftDetailPanel menampilkan 4 tab stub (Ringkasan, Skor, Saran AI, Tata Bahasa) dengan placeholder content"
    - "LeftPanelToggle button ada di left edge center panel, toggle leftPanelOpen di Zustand store, aria-expanded benar"
    - "RightRailStats menampilkan skor keseluruhan (32px bold lime), suggestion count, grammar count, dan 'Asisten chat segera hadir'"
    - "WorkspaceV2Shell (shell.tsx) diupdate: PdfViewerPanel + LeftDetailPanel + LeftPanelToggle + RightRailStats sudah terpasang"
    - "page.tsx fetch getJobFileUrl setelah hydration dan meneruskan pdfUrl ke shell"
    - "loading.tsx menggunakan WorkspaceSkeleton component"
    - "getJobFileUrl ada di lib/workspace.ts"
    - "Interface bisa dinavigasi: /workspace-v2/[job_id] merender PDF (jika file_url tersedia) dan layout 3-panel lengkap"
  artifacts:
    - path: "frontend/components/workspace-v2/left-detail-panel.tsx"
      provides: "Left panel dengan 4 stub tabs dan empty state"
      contains: "Detail Analisis"
    - path: "frontend/components/workspace-v2/left-panel-toggle.tsx"
      provides: "Toggle button dengan aria-expanded dan 44px touch target"
      contains: "aria-expanded"
    - path: "frontend/components/workspace-v2/right-rail-stats.tsx"
      provides: "Right rail dengan score card + stat rows + chat stub"
      contains: "Ringkasan"
    - path: "frontend/components/workspace-v2/workspace-skeleton.tsx"
      provides: "Full 3-panel loading skeleton"
      contains: "animate-pulse"
    - path: "frontend/lib/workspace.ts"
      provides: "getJobFileUrl function"
      exports: ["getJobFileUrl", "WorkspaceFileUrl"]
  key_links:
    - from: "frontend/app/workspace-v2/[job_id]/page.tsx"
      to: "frontend/lib/workspace.ts"
      via: "getJobFileUrl(job_id) call"
      pattern: "getJobFileUrl"
    - from: "frontend/components/workspace-v2/shell.tsx"
      to: "frontend/components/workspace-v2/pdf-viewer-panel.tsx"
      via: "render PdfViewerPanel dengan pdfUrl"
      pattern: "PdfViewerPanel"
    - from: "frontend/components/workspace-v2/left-panel-toggle.tsx"
      to: "frontend/lib/stores/workspace-v2-store.ts"
      via: "toggleLeftPanel() dari useWorkspaceV2Store"
      pattern: "toggleLeftPanel"
---

<objective>
Lengkapi workspace-v2 shell dengan semua panel components: LeftDetailPanel (4 stub tabs), LeftPanelToggle, RightRailStats (summary stats dari hydration), WorkspaceSkeleton. Update shell.tsx untuk wire semua components. Update page.tsx untuk fetch pdfUrl. Tambahkan getJobFileUrl ke lib/workspace.ts.

Purpose: Setelah plan ini selesai, `/workspace-v2/[job_id]` adalah halaman yang fully functional — PDF terload (jika R2 tersedia), layout 3-panel lengkap, toggle panel bekerja, stats kanan tampil, skeleton saat loading.

Output: Workspace-v2 route fully operational dengan semua 3 panel terpasang.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-first-workspace-shell/13-CONTEXT.md
@.planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md
@.planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md
@frontend/components/workspace-v2/shell.tsx
@frontend/app/workspace-v2/[job_id]/page.tsx
@frontend/lib/workspace.ts
@frontend/lib/stores/workspace-v2-store.ts

<interfaces>
<!-- Shell.tsx yang dibuat di Plan 03 — akan diupdate task 2 plan ini -->
<!-- Shell sudah memiliki: aside placeholders untuk left/right, main placeholder untuk center -->
<!-- Update: ganti placeholder divs dengan actual components -->

<!-- WorkspaceHydration analysis fields untuk RightRailStats -->
```typescript
// analysis.scores.overall: number (0-100) — score keseluruhan
// analysis.suggestions: SuggestionCard[] | null — array per section
// analysis.grammar_issues: sudah diproses di backend — tapi bisa dari hydration.analysis
// Untuk suggestion count: hitung dari semua items di semua cards
// SuggestionCard: { section: string, suggestions: SuggestionItem[] }
// Total suggestion count = suggestions.flatMap(card => card.suggestions).length
```

<!-- WorkspaceFileUrl interface yang ditambahkan ke lib/workspace.ts -->
```typescript
export interface WorkspaceFileUrl {
  file_url: string;
  expires_in: number;
}
export async function getJobFileUrl(jobId: string): Promise<WorkspaceFileUrl>;
// Menggunakan apiFetch<WorkspaceFileUrl>(`/jobs/${jobId}/file`)
```

<!-- Zustand store interface dari Plan 03 -->
```typescript
// useWorkspaceV2Store() returns:
// leftPanelOpen: boolean
// toggleLeftPanel: () => void
// pdfUrl: string | null
// hydration: WorkspaceHydration | null
// setPdfUrl: (url: string | null) => void
```

<!-- UI-SPEC values untuk left panel toggle -->
<!-- Toggle position: absolute left-0 top-1/2 -translate-y-1/2 z-10 di center panel -->
<!-- Toggle size: 28x28px visual, 44x44px touch target via before: pseudo-element -->
<!-- Toggle icon: ChevronRight (collapsed) / ChevronLeft (expanded) — 14px -->
<!-- Toggle bg: bg-[--ws-surface] hover:bg-[--ws-surface-hover] -->
<!-- Touch target: before:content-[''] before:absolute before:inset-[-8px] -->

<!-- UI-SPEC values untuk right rail score -->
<!-- Score number: text-[32px] font-bold leading-none text-[--ws-accent] -->
<!-- Score container: bg-[--ws-surface-active] border border-[--ws-border-accent] rounded-xl p-4 mb-4 -->
<!-- Score label: text-xs font-bold uppercase tracking-widest text-[--ws-ink-ghost] -->
<!-- Stat row: flex items-center gap-3 py-3 border-b border-[--ws-border] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buat LeftDetailPanel, LeftPanelToggle, RightRailStats, WorkspaceSkeleton</name>
  <read_first>
    - .planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md (Section 2, 3, 5, 6 — panel contracts)
    - frontend/lib/stores/workspace-v2-store.ts (untuk toggleLeftPanel dan leftPanelOpen)
    - frontend/lib/workspace.ts (untuk WorkspaceHydration interface dan analysis types)
  </read_first>
  <files>
    frontend/components/workspace-v2/left-detail-panel.tsx,
    frontend/components/workspace-v2/left-panel-toggle.tsx,
    frontend/components/workspace-v2/right-rail-stats.tsx,
    frontend/components/workspace-v2/workspace-skeleton.tsx
  </files>
  <action>
**1. `frontend/components/workspace-v2/left-detail-panel.tsx`** — Left panel stub tabs:
```tsx
"use client";
/**
 * LeftDetailPanel — left panel workspace-v2.
 * Phase 13: stub tabs saja — data wiring di Phase 15.
 * Tab labels: Ringkasan, Skor, Saran AI, Tata Bahasa.
 */
import { useState } from "react";
import { cn } from "@/lib/utils";

type TabId = "ringkasan" | "skor" | "saran" | "tatabahasa";

const TABS: { id: TabId; label: string }[] = [
  { id: "ringkasan", label: "Ringkasan" },
  { id: "skor", label: "Skor" },
  { id: "saran", label: "Saran AI" },
  { id: "tatabahasa", label: "Tata Bahasa" },
];

interface LeftDetailPanelProps {
  className?: string;
}

export function LeftDetailPanel({ className }: LeftDetailPanelProps) {
  const [activeTab, setActiveTab] = useState<TabId>("ringkasan");

  return (
    <div className={cn("flex flex-col h-full bg-[--ws-surface]", className)}>
      {/* Panel header */}
      <div className="flex items-center justify-between p-4 border-b border-[--ws-border]">
        <h2 className="text-[15px] font-bold text-[--ws-ink]">
          Detail Analisis
        </h2>
      </div>

      {/* Tab buttons — visual only, Phase 13 stub */}
      <div className="flex flex-wrap gap-1.5 px-4 pt-3 pb-2">
        {TABS.map((tab) => (
          <button
            key={tab.id}
            onClick={() => setActiveTab(tab.id)}
            className={cn(
              "rounded-full px-3 py-1.5 text-xs font-bold uppercase tracking-widest transition-colors duration-150",
              activeTab === tab.id
                ? "bg-[--ws-surface-active] text-[--ws-ink-secondary]"
                : "border border-[--ws-border] bg-transparent text-[--ws-ink-ghost] hover:text-[--ws-ink-secondary]"
            )}
          >
            {tab.label}
          </button>
        ))}
      </div>

      {/* Tab content — Phase 13 placeholder */}
      <div className="flex-1 overflow-y-auto p-4">
        <div className="rounded-xl border border-[--ws-border] bg-[--ws-surface-active] p-4">
          <p className="text-xs leading-relaxed text-[--ws-ink-ghost]">
            Detail analisis tersedia setelah proses selesai. Panel ini akan diisi pada Fase 15.
          </p>
        </div>
      </div>
    </div>
  );
}
```

**2. `frontend/components/workspace-v2/left-panel-toggle.tsx`** — Toggle button di edge center panel:
```tsx
"use client";
/**
 * LeftPanelToggle — toggle button untuk left detail panel.
 * Posisi: absolute left-0 top-1/2 pada center PDF panel (diatur dari parent).
 * Ukuran visual 28x28, touch target 44x44 via before: pseudo-element.
 * (UI-SPEC Section 3)
 */
import { ChevronLeft, ChevronRight } from "lucide-react";
import { useWorkspaceV2Store } from "@/lib/stores/workspace-v2-store";
import { cn } from "@/lib/utils";

interface LeftPanelToggleProps {
  className?: string;
}

export function LeftPanelToggle({ className }: LeftPanelToggleProps) {
  const { leftPanelOpen, toggleLeftPanel } = useWorkspaceV2Store();

  return (
    <button
      onClick={toggleLeftPanel}
      aria-label={leftPanelOpen ? "Tutup detail panel" : "Buka detail panel"}
      aria-expanded={leftPanelOpen}
      className={cn(
        // Ukuran visual 28x28
        "relative flex h-7 w-7 items-center justify-center",
        // Styling
        "rounded-full border border-[--ws-border] bg-[--ws-surface] shadow-md",
        "text-[--ws-ink-secondary] transition-colors duration-150",
        "hover:bg-[--ws-surface-hover]",
        // Accessibility: touch target 44x44 via before pseudo-element
        "before:absolute before:inset-[-8px] before:content-['']",
        className
      )}
    >
      {leftPanelOpen ? (
        <ChevronLeft className="h-3.5 w-3.5" aria-hidden="true" />
      ) : (
        <ChevronRight className="h-3.5 w-3.5" aria-hidden="true" />
      )}
    </button>
  );
}
```

**3. `frontend/components/workspace-v2/right-rail-stats.tsx`** — Summary stats panel:
```tsx
"use client";
/**
 * RightRailStats — right rail dengan summary stats.
 * Phase 13: skor keseluruhan, jumlah saran, jumlah isu grammar, chat coming soon.
 * Data dari WorkspaceHydration.analysis.
 * (UI-SPEC Section 5 — Right Rail Stats Design Contract)
 */
import { Lightbulb, FileText } from "lucide-react";
import { useWorkspaceV2Store } from "@/lib/stores/workspace-v2-store";
import { cn } from "@/lib/utils";

interface RightRailStatsProps {
  className?: string;
}

export function RightRailStats({ className }: RightRailStatsProps) {
  const { hydration } = useWorkspaceV2Store();

  const analysis = hydration?.analysis;

  // Hitung total suggestion count dari semua section cards
  const suggestionCount = analysis?.suggestions
    ? analysis.suggestions.flatMap((card) => card.suggestions).length
    : 0;

  // Grammar issues count
  const grammarCount = Array.isArray(analysis?.ats_checks)
    ? 0  // ats_checks bukan grammar — gunakan 0 jika grammar_issues tidak tersedia di hydration
    : 0;
  // NOTE: grammar_issues tidak ada di WorkspaceAnalysisContext saat ini.
  // Akan ditambahkan di Phase 15. Sementara ini tampilkan 0.

  const overallScore = analysis?.scores?.overall ?? null;

  return (
    <div
      className={cn(
        "flex flex-col h-full bg-[--ws-surface] border-l border-[--ws-border] overflow-y-auto p-4",
        className
      )}
    >
      {/* Panel title */}
      <h2 className="mb-4 text-[15px] font-bold text-[--ws-ink]">
        Ringkasan
      </h2>

      {/* Score card */}
      <div
        className="mb-4 rounded-xl border border-[--ws-border-accent] bg-[--ws-surface-active] p-4"
        aria-label={`Skor Keseluruhan: ${overallScore ?? 0} dari 100`}
      >
        <p className="mb-1 text-xs font-bold uppercase tracking-widest text-[--ws-ink-ghost]">
          Skor Keseluruhan
        </p>
        <div className="flex items-baseline gap-1">
          <span className="text-[32px] font-bold leading-none text-[--ws-accent]">
            {overallScore ?? "—"}
          </span>
          <span className="text-sm font-normal text-[--ws-ink-ghost]">
            / 100
          </span>
        </div>
      </div>

      {/* Stat rows */}
      {/* AI Suggestions */}
      <div className="flex items-center gap-3 border-b border-[--ws-border] py-3">
        <Lightbulb
          className="h-3.5 w-3.5 flex-none text-[--ws-accent]"
          aria-hidden="true"
        />
        <div className="flex items-baseline gap-1.5">
          <span className="text-[15px] font-bold text-[--ws-ink]">
            {suggestionCount}
          </span>
          <span className="text-xs text-[--ws-ink-secondary]">
            saran perbaikan
          </span>
        </div>
      </div>

      {/* Grammar Issues */}
      <div className="flex items-center gap-3 border-b border-[--ws-border] py-3">
        <FileText
          className="h-3.5 w-3.5 flex-none text-[--ws-ink-secondary]"
          aria-hidden="true"
        />
        <div className="flex items-baseline gap-1.5">
          <span className="text-[15px] font-bold text-[--ws-ink]">
            {grammarCount}
          </span>
          <span className="text-xs text-[--ws-ink-secondary]">
            isu tata bahasa
          </span>
        </div>
      </div>

      {/* Divider */}
      <div className="my-4 h-px w-full bg-[--ws-border]" aria-hidden="true" />

      {/* Chat stub */}
      <p className="text-center text-xs leading-relaxed text-[--ws-ink-ghost]">
        Asisten chat segera hadir
      </p>
    </div>
  );
}
```

**4. `frontend/components/workspace-v2/workspace-skeleton.tsx`** — Full 3-panel loading skeleton:
```tsx
/**
 * WorkspaceSkeleton — loading skeleton untuk seluruh workspace-v2.
 * Digunakan di loading.tsx sebagai route-level loading state.
 * Tiga kolom sesuai proporsi grid produksi.
 * (UI-SPEC Section 6)
 */
export function WorkspaceSkeleton() {
  return (
    <div
      data-workspace-v2
      className="flex h-screen flex-col overflow-hidden bg-[#111111]"
      aria-busy="true"
      aria-live="polite"
    >
      {/* Header skeleton */}
      <div className="flex h-12 flex-none items-center gap-3 border-b border-[rgba(255,255,255,0.13)] bg-[#111111] px-4">
        <div className="h-4 w-16 animate-pulse rounded bg-[#1A1A1A]" />
        <div className="h-4 w-px bg-[rgba(255,255,255,0.13)]" />
        <div className="h-4 w-48 animate-pulse rounded bg-[#1A1A1A]" />
      </div>

      {/* Body skeleton — 3 columns */}
      <div className="flex flex-1 overflow-hidden">
        {/* Left column skeleton (290px — hidden on mobile) */}
        <div className="hidden w-[290px] flex-none flex-col gap-3 border-r border-[rgba(255,255,255,0.07)] bg-[#1A1A1A] p-4 lg:flex">
          <div className="h-5 w-32 animate-pulse rounded bg-[#222222]" />
          <div className="h-4 w-full animate-pulse rounded bg-[#222222]" />
          <div className="h-4 w-3/4 animate-pulse rounded bg-[#222222]" />
        </div>

        {/* Center column skeleton */}
        <div className="flex flex-1 items-center justify-center bg-[#111111] p-6">
          <div className="mx-auto w-full max-w-[860px] min-h-[400px] animate-pulse rounded-2xl bg-[#1A1A1A]" />
        </div>

        {/* Right column skeleton (340px — hidden on mobile) */}
        <div className="hidden w-[340px] flex-none flex-col gap-3 border-l border-[rgba(255,255,255,0.07)] bg-[#1A1A1A] p-4 lg:flex">
          <div className="h-5 w-24 animate-pulse rounded bg-[#222222]" />
          <div className="h-20 w-full animate-pulse rounded-xl bg-[#222222]" />
          <div className="h-4 w-full animate-pulse rounded bg-[#222222]" />
          <div className="h-4 w-3/4 animate-pulse rounded bg-[#222222]" />
        </div>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | grep -E "left-detail|left-panel-toggle|right-rail|workspace-skeleton|error TS" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Detail Analisis" frontend/components/workspace-v2/left-detail-panel.tsx` → ditemukan (panel title)
    - `grep -n "Ringkasan\|Skor\|Saran AI\|Tata Bahasa" frontend/components/workspace-v2/left-detail-panel.tsx` → ditemukan (4 tab labels)
    - `grep -n "Panel ini akan diisi pada Fase 15" frontend/components/workspace-v2/left-detail-panel.tsx` → ditemukan (stub copy)
    - `grep -n "aria-expanded" frontend/components/workspace-v2/left-panel-toggle.tsx` → ditemukan
    - `grep -n "Buka detail panel\|Tutup detail panel" frontend/components/workspace-v2/left-panel-toggle.tsx` → ditemukan (aria-label values)
    - `grep -n "toggleLeftPanel" frontend/components/workspace-v2/left-panel-toggle.tsx` → ditemukan (connects to store)
    - `grep -n "before:absolute before:inset-\[-8px\]" frontend/components/workspace-v2/left-panel-toggle.tsx` → ditemukan (44px touch target)
    - `grep -n "Ringkasan" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan (panel title)
    - `grep -n "Skor Keseluruhan" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan (score label)
    - `grep -n "text-\[32px\] font-bold" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan (score display)
    - `grep -n "saran perbaikan" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan
    - `grep -n "isu tata bahasa" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan
    - `grep -n "Asisten chat segera hadir" frontend/components/workspace-v2/right-rail-stats.tsx` → ditemukan
    - `grep -n "animate-pulse" frontend/components/workspace-v2/workspace-skeleton.tsx` → ditemukan minimal 5 kali
    - `grep -n "data-workspace-v2" frontend/components/workspace-v2/workspace-skeleton.tsx` → ditemukan (scoped CSS vars)
  </acceptance_criteria>
  <done>
    LeftDetailPanel dengan 4 tab stub. LeftPanelToggle dengan aria-expanded dan 44px touch target. RightRailStats dengan score card (32px lime), suggestion/grammar counts, chat stub. WorkspaceSkeleton dengan 3-panel proporsi.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire shell.tsx, update page.tsx + loading.tsx, tambahkan getJobFileUrl ke lib/workspace.ts</name>
  <read_first>
    - frontend/components/workspace-v2/shell.tsx (versi dari Plan 03 — akan diupdate)
    - frontend/app/workspace-v2/[job_id]/page.tsx (versi dari Plan 03 — akan diupdate)
    - frontend/app/workspace-v2/[job_id]/loading.tsx (versi dari Plan 03 — akan diupdate)
    - frontend/lib/workspace.ts (untuk memahami apiFetch dan existing functions)
  </read_first>
  <files>
    frontend/components/workspace-v2/shell.tsx,
    frontend/app/workspace-v2/[job_id]/page.tsx,
    frontend/app/workspace-v2/[job_id]/loading.tsx,
    frontend/lib/workspace.ts
  </files>
  <action>
**1. Update `frontend/lib/workspace.ts`** — tambahkan getJobFileUrl di bagian bawah file:

```typescript
// Phase 13: file presigned URL fetch (PDF-02)
export interface WorkspaceFileUrl {
  file_url: string;
  expires_in: number;
}

export async function getJobFileUrl(jobId: string): Promise<WorkspaceFileUrl> {
  return apiFetch<WorkspaceFileUrl>(`/jobs/${jobId}/file`);
}
```

**2. Update `frontend/app/workspace-v2/[job_id]/page.tsx`** — tambahkan pdfUrl fetch:

Ganti konten page.tsx dengan versi ini:
```tsx
import { getWorkspaceHydration, getJobFileUrl } from "@/lib/workspace";
import { WorkspaceV2Shell } from "@/components/workspace-v2/shell";

interface WorkspaceV2PageProps {
  params: Promise<{ job_id: string }>;
}

export default async function WorkspaceV2Page({ params }: WorkspaceV2PageProps) {
  const { job_id } = await params;

  // Fetch hydration dan file URL secara paralel (PDF-03: defaults to optimized view)
  const [hydration, fileUrlResult] = await Promise.allSettled([
    getWorkspaceHydration(job_id),
    getJobFileUrl(job_id),
  ]);

  const hydratedData = hydration.status === "fulfilled" ? hydration.value : null;
  const pdfUrl = fileUrlResult.status === "fulfilled" ? fileUrlResult.value.file_url : null;

  return (
    <WorkspaceV2Shell
      jobId={job_id}
      hydration={hydratedData}
      initialPdfUrl={pdfUrl}
    />
  );
}
```

**3. Update `frontend/components/workspace-v2/shell.tsx`** — ganti placeholder divs dengan actual components:

Ganti konten shell.tsx. Props baru: tambahkan `initialPdfUrl?: string | null`.

Perubahan utama dari versi Plan 03:
- Tambahkan `initialPdfUrl` prop dan inisialisasi di useEffect: `if (initialPdfUrl) setPdfUrl(initialPdfUrl)`
- Ganti `<aside>` placeholder left panel dengan `<LeftDetailPanel />`
- Tambahkan `<LeftPanelToggle>` dengan posisi absolute di `<main>` center panel
- Ganti `<main>` center placeholder dengan `<PdfViewerPanel pdfUrl={pdfUrl} />`
- Ganti `<aside>` placeholder right rail dengan `<RightRailStats />`

Konten lengkap shell.tsx yang diupdate:

```tsx
"use client";
import { useEffect } from "react";
import { cn } from "@/lib/utils";
import { useWorkspaceV2Store } from "@/lib/stores/workspace-v2-store";
import { WorkspaceV2Header } from "./header";
import { LeftDetailPanel } from "./left-detail-panel";
import { LeftPanelToggle } from "./left-panel-toggle";
import { PdfViewerPanel } from "./pdf-viewer-panel";
import { RightRailStats } from "./right-rail-stats";
import type { WorkspaceHydration } from "@/lib/workspace";

interface WorkspaceV2ShellProps {
  jobId: string;
  hydration: WorkspaceHydration | null;
  initialPdfUrl?: string | null;
}

export function WorkspaceV2Shell({
  jobId,
  hydration,
  initialPdfUrl,
}: WorkspaceV2ShellProps) {
  const { leftPanelOpen, pdfUrl, setJobId, setHydration, setPdfUrl } =
    useWorkspaceV2Store();

  useEffect(() => {
    setJobId(jobId);
    if (hydration) setHydration(hydration);
    if (initialPdfUrl) setPdfUrl(initialPdfUrl);
  }, [jobId, hydration, initialPdfUrl, setJobId, setHydration, setPdfUrl]);

  return (
    <div
      data-workspace-v2
      className="flex h-screen flex-col overflow-hidden bg-[--ws-bg] text-[--ws-ink]"
    >
      {/* Header h-12 */}
      <WorkspaceV2Header
        filename={hydration?.file.filename ?? null}
        jobId={jobId}
        jobStatus={hydration?.status}
        className="h-12 flex-none border-b border-[--ws-border-strong]"
      />

      {/* Body — 3-panel grid on desktop, single column on mobile */}
      <div
        className={cn(
          "flex-1 overflow-hidden",
          // Grid transitions on left panel open/close
          leftPanelOpen
            ? "grid lg:grid-cols-[290px_minmax(0,1fr)_340px]"
            : "grid lg:grid-cols-[0px_minmax(0,1fr)_340px]",
          "transition-[grid-template-columns] duration-200 ease-in-out"
        )}
      >
        {/* Left detail panel — hidden mobile, collapsed by default */}
        <aside
          className={cn(
            "hidden lg:flex flex-col overflow-hidden",
            leftPanelOpen
              ? "w-[290px] opacity-100"
              : "w-0 overflow-hidden opacity-0 pointer-events-none",
            "transition-[width,opacity] duration-200 ease-in-out"
          )}
          aria-hidden={!leftPanelOpen}
        >
          <LeftDetailPanel className="h-full" />
        </aside>

        {/* Center PDF panel — full width on mobile */}
        <main className="relative flex flex-col overflow-hidden">
          {/* Toggle button — absolute, left edge of center panel */}
          <div className="absolute left-0 top-1/2 z-10 -translate-y-1/2 hidden lg:block">
            <LeftPanelToggle />
          </div>

          {/* PDF viewer — fills remaining space */}
          <PdfViewerPanel pdfUrl={pdfUrl} />
        </main>

        {/* Right rail — hidden on mobile */}
        <aside className="hidden lg:flex flex-col">
          <RightRailStats className="h-full" />
        </aside>
      </div>
    </div>
  );
}
```

**4. Update `frontend/app/workspace-v2/[job_id]/loading.tsx`** — gunakan WorkspaceSkeleton:

```tsx
import { WorkspaceSkeleton } from "@/components/workspace-v2/workspace-skeleton";

export default function WorkspaceV2Loading() {
  return <WorkspaceSkeleton />;
}
```
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getJobFileUrl" frontend/lib/workspace.ts` → ditemukan (function exported)
    - `grep -n "WorkspaceFileUrl" frontend/lib/workspace.ts` → ditemukan (interface exported)
    - `grep -n "getJobFileUrl" frontend/app/workspace-v2/\[job_id\]/page.tsx` → ditemukan (digunakan di page)
    - `grep -n "Promise.allSettled" frontend/app/workspace-v2/\[job_id\]/page.tsx` → ditemukan (parallel fetch)
    - `grep -n "initialPdfUrl" frontend/components/workspace-v2/shell.tsx` → ditemukan (prop accepted)
    - `grep -n "PdfViewerPanel" frontend/components/workspace-v2/shell.tsx` → ditemukan (imported dan dirender)
    - `grep -n "LeftDetailPanel" frontend/components/workspace-v2/shell.tsx` → ditemukan
    - `grep -n "LeftPanelToggle" frontend/components/workspace-v2/shell.tsx` → ditemukan
    - `grep -n "RightRailStats" frontend/components/workspace-v2/shell.tsx` → ditemukan
    - `grep -n "WorkspaceSkeleton" frontend/app/workspace-v2/\[job_id\]/loading.tsx` → ditemukan
    - `npx tsc --noEmit` → exit 0 (TypeScript clean)
  </acceptance_criteria>
  <done>
    getJobFileUrl ditambahkan ke lib/workspace.ts. page.tsx fetch pdfUrl secara paralel dengan hydration menggunakan Promise.allSettled. shell.tsx menyertakan semua 4 components (LeftDetailPanel, LeftPanelToggle, PdfViewerPanel, RightRailStats). loading.tsx menggunakan WorkspaceSkeleton.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx → getJobFileUrl | Promise.allSettled — URL fetch failure ditangkap gracefully |
| pdfUrl → PdfViewerPanel | null URL ditangkap di PdfViewer dengan "PDF tidak tersedia" message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Information Disclosure | pdfUrl di client store | accept | pdfUrl adalah presigned URL dengan expiry 3600s; tidak ada token/credential di URL; disimpan hanya di Zustand (client memory, tidak di LocalStorage) |
| T-13-05-02 | Denial of Service | Promise.allSettled parallel fetch | mitigate | allSettled tidak throw jika salah satu gagal; null pdfUrl ditampilkan sebagai "PDF tidak tersedia" — tidak crash halaman |
| T-13-05-03 | Elevation of Privilege | LeftPanelToggle aria-expanded | accept | Toggle hanya mengubah UI state (leftPanelOpen boolean) di Zustand store; tidak ada data mutation |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit
```
Harus exit 0.

```bash
# Verifikasi wiring
grep -n "PdfViewerPanel\|LeftDetailPanel\|LeftPanelToggle\|RightRailStats" frontend/components/workspace-v2/shell.tsx
grep -n "getJobFileUrl" frontend/lib/workspace.ts frontend/app/workspace-v2/\[job_id\]/page.tsx
```
Semua harus ditemukan.

Manual test (jika dev server tersedia):
Buka `http://localhost:3000/workspace-v2/[valid_job_id]`
- Header tampil dengan filename dan tombol Kembali
- Toggle button tampil di left edge center panel
- Right rail tampil dengan "Ringkasan" dan score
- Skeleton tampil saat loading
</verification>

<success_criteria>
- LeftDetailPanel dengan 4 tab stub, empty state copy Indonesian
- LeftPanelToggle dengan aria-expanded, aria-label Indonesian, 44px touch target
- RightRailStats dengan score 32px lime, suggestion count, grammar count, "Asisten chat segera hadir"
- WorkspaceSkeleton 3-panel animate-pulse
- shell.tsx wire semua 4 components
- page.tsx fetch pdfUrl + hydration paralel dengan Promise.allSettled
- loading.tsx menggunakan WorkspaceSkeleton
- getJobFileUrl di lib/workspace.ts
- TypeScript clean
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/13-pdf-first-workspace-shell/13-05-SUMMARY.md` dengan:
- Konfirmasi semua 8 file modified berhasil diupdate
- Hasil tsc --noEmit
- Manual test result: apakah /workspace-v2/[job_id] bisa diakses dan layout tampil benar
- Apakah pdfUrl berhasil diload (atau null jika R2 tidak dikonfigurasi)
- Any issues dengan Zustand store initialization
</output>
