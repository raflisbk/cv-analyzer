---
phase: 13-pdf-first-workspace-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/app/globals.css
  - frontend/lib/stores/workspace-v2-store.ts
  - frontend/app/workspace-v2/[job_id]/layout.tsx
  - frontend/app/workspace-v2/[job_id]/loading.tsx
  - frontend/app/workspace-v2/[job_id]/page.tsx
  - frontend/components/workspace-v2/shell.tsx
  - frontend/components/workspace-v2/header.tsx
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - PDF-03

must_haves:
  truths:
    - "Route /workspace-v2/[job_id] bisa diakses (tidak 404) dan merender WorkspaceV2Shell"
    - "CSS variables [data-workspace-v2] didefinisikan di globals.css — --ws-bg, --ws-surface, --ws-accent, dll."
    - "Zustand store useWorkspaceV2Store mengekspor leftPanelOpen (default false), viewMode (default 'optimized'), toggleLeftPanel, setPdfUrl, setHydration"
    - "Shell menampilkan 3-panel grid di desktop (lg:grid-cols-[290px_minmax(0,1fr)_340px]) dan single column di mobile"
    - "Header h-12 menampilkan tombol Kembali + filename dari file_info.filename"
    - "Left panel collapsed by default (leftPanelOpen: false dalam store)"
    - "Route /workspace dan semua files di workspace/canvas/ tidak disentuh"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "CSS custom properties untuk workspace-v2 theme"
      contains: "--ws-bg"
    - path: "frontend/lib/stores/workspace-v2-store.ts"
      provides: "Zustand store state management untuk workspace-v2"
      exports: ["useWorkspaceV2Store"]
    - path: "frontend/app/workspace-v2/[job_id]/page.tsx"
      provides: "Server component untuk workspace-v2 route"
      contains: "WorkspaceV2Shell"
    - path: "frontend/components/workspace-v2/shell.tsx"
      provides: "Root layout component dengan 3-panel grid"
      contains: "data-workspace-v2"
    - path: "frontend/components/workspace-v2/header.tsx"
      provides: "Minimal h-12 header dengan back button dan filename"
      contains: "Kembali"
  key_links:
    - from: "frontend/app/workspace-v2/[job_id]/page.tsx"
      to: "frontend/components/workspace-v2/shell.tsx"
      via: "import dan render"
      pattern: "WorkspaceV2Shell"
    - from: "frontend/components/workspace-v2/shell.tsx"
      to: "frontend/lib/stores/workspace-v2-store.ts"
      via: "useWorkspaceV2Store() hook"
      pattern: "useWorkspaceV2Store"
---

<objective>
Buat fondasi frontend untuk workspace-v2: CSS variables tema gelap, Zustand store, route Next.js App Router, WorkspaceV2Shell (3-panel grid), dan WorkspaceV2Header (header minimal h-12).

Purpose: Ini adalah skeleton shell yang bisa dinavigasi. Setelah plan ini selesai, `/workspace-v2/[any_job_id]` bisa dibuka di browser dan menampilkan layout 3-panel dengan header, meskipun PDF viewer dan panel konten belum ada (placeholder divs).

Output: Route /workspace-v2/[job_id] live, CSS vars terdefinisi, Zustand store ready, Shell + Header components.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-first-workspace-shell/13-CONTEXT.md
@.planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md
@.planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md
@frontend/app/globals.css
@frontend/app/workspace/[job_id]/page.tsx
@frontend/app/workspace/[job_id]/loading.tsx
@frontend/lib/workspace.ts

<interfaces>
<!-- WorkspaceHydration type dari lib/workspace.ts (sudah ada) -->
```typescript
export interface WorkspaceHydration {
  job_id: string;
  status: "preparing" | "ready" | "failed";
  file: WorkspaceFileInfo;  // { filename: string|null, mime_type, size, extension }
  document: WorkspaceDocumentPayload;
  analysis: WorkspaceAnalysisContext;
  navigation: WorkspaceNavigation;
  error?: string | null;
}

export async function getWorkspaceHydration(jobId: string): Promise<WorkspaceHydration>;
```

<!-- Zustand 5.x API (package terinstall di Plan 01) -->
```typescript
import { create } from "zustand";
// create<StateType>((set) => ({ ...initialState, ...actions }))
```

<!-- WorkspaceFileUrl akan ditambahkan ke lib/workspace.ts di Plan 05 -->
<!-- Di Plan 03 ini, page.tsx hanya fetch hydration, bukan file URL -->

<!-- CSS variables scope: [data-workspace-v2] attribute selector -->
<!-- Hindari override :root vars — scoped ke workspace-v2 container saja -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS variables workspace-v2 di globals.css dan Zustand store</name>
  <read_first>
    - frontend/app/globals.css (untuk memahami struktur :root vars yang sudah ada dan menghindari konflik)
    - frontend/lib/workspace.ts (untuk WorkspaceHydration type yang dipakai di store)
    - .planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md (Section: Workspace-v2 Color Tokens)
  </read_first>
  <files>
    frontend/app/globals.css,
    frontend/lib/stores/workspace-v2-store.ts
  </files>
  <action>
**Update `frontend/app/globals.css`** — tambahkan CSS custom properties DI BAWAH `@layer base` yang ada, sebagai selector baru (bukan di dalam :root):

```css
/* ============================================================
   WORKSPACE-V2 THEME TOKENS (Phase 13)
   Scoped ke [data-workspace-v2] — tidak override landing page tokens
   ============================================================ */
[data-workspace-v2] {
  /* Shell surfaces */
  --ws-bg:             #111111;   /* 60% dominant — outer shell, header bg */
  --ws-surface:        #1A1A1A;   /* 30% secondary — left panel, right rail bg */
  --ws-surface-hover:  #222222;   /* interactive surface hover state */
  --ws-surface-active: #282828;   /* pressed/selected surface */

  /* Borders */
  --ws-border:         rgba(255, 255, 255, 0.07);  /* default panel borders */
  --ws-border-accent:  rgba(202, 255, 67, 0.14);   /* lime-tinted accent border */
  --ws-border-strong:  rgba(255, 255, 255, 0.13);  /* header bottom, focus rings */

  /* PDF Paper */
  --ws-paper:          #FFFDF4;
  --ws-paper-shadow:   0 8px 48px rgba(0, 0, 0, 0.65);

  /* Typography on dark */
  --ws-ink:            #F5F2D8;   /* primary text */
  --ws-ink-secondary:  rgba(245, 242, 216, 0.65);
  --ws-ink-ghost:      rgba(245, 242, 216, 0.35);

  /* Accent */
  --ws-accent:         #CAFF43;
  --ws-accent-fg:      #111111;
  --ws-accent-muted:   rgba(202, 255, 67, 0.12);

  /* Destructive */
  --ws-destructive:    hsl(0 84.2% 60.2%);
}
```

**Buat direktori dan file `frontend/lib/stores/workspace-v2-store.ts`:**
(Pastikan direktori `frontend/lib/stores/` dibuat)

```typescript
/**
 * Zustand store untuk workspace-v2.
 * Phase 13: state minimal — leftPanelOpen, pdfUrl, viewMode, hydration context.
 * Jangan gunakan persist middleware — Yjs menangani persistence.
 */
"use client";
import { create } from "zustand";
import type { WorkspaceHydration } from "@/lib/workspace";

interface WorkspaceV2State {
  // PDF state
  pdfUrl: string | null;
  viewMode: "optimized" | "original"; // PDF-03: default optimized

  // Layout state
  leftPanelOpen: boolean; // CONTEXT.md: collapsed by default = false

  // Job context
  jobId: string;
  hydration: WorkspaceHydration | null;

  // Actions
  setPdfUrl: (url: string | null) => void;
  setViewMode: (mode: "optimized" | "original") => void;
  toggleLeftPanel: () => void;
  setJobId: (jobId: string) => void;
  setHydration: (hydration: WorkspaceHydration) => void;
}

export const useWorkspaceV2Store = create<WorkspaceV2State>((set) => ({
  pdfUrl: null,
  viewMode: "optimized",   // PDF-03: workspace defaults ke optimized PDF view
  leftPanelOpen: false,    // CONTEXT.md D-01: collapsed by default
  jobId: "",
  hydration: null,

  setPdfUrl: (url) => set({ pdfUrl: url }),
  setViewMode: (mode) => set({ viewMode: mode }),
  toggleLeftPanel: () => set((state) => ({ leftPanelOpen: !state.leftPanelOpen })),
  setJobId: (jobId) => set({ jobId }),
  setHydration: (hydration) => set({ hydration }),
}));
```
  </action>
  <verify>
    <automated>
      cd frontend && node -e "const fs=require('fs'); const css=fs.readFileSync('app/globals.css','utf8'); console.log('--ws-bg present:', css.includes('--ws-bg')); console.log('--ws-accent present:', css.includes('--ws-accent')); console.log('data-workspace-v2 present:', css.includes('[data-workspace-v2]'));"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[data-workspace-v2\]" frontend/app/globals.css` → ditemukan
    - `grep -n "\-\-ws-bg" frontend/app/globals.css` → ditemukan dengan nilai `#111111`
    - `grep -n "\-\-ws-surface:" frontend/app/globals.css` → ditemukan dengan nilai `#1A1A1A`
    - `grep -n "\-\-ws-accent:" frontend/app/globals.css` → ditemukan dengan nilai `#CAFF43`
    - `grep -n "\-\-ws-paper:" frontend/app/globals.css` → ditemukan dengan nilai `#FFFDF4`
    - File `frontend/lib/stores/workspace-v2-store.ts` ada
    - `grep -n "export const useWorkspaceV2Store" frontend/lib/stores/workspace-v2-store.ts` → ditemukan
    - `grep -n "leftPanelOpen: false" frontend/lib/stores/workspace-v2-store.ts` → ditemukan (default collapsed)
    - `grep -n 'viewMode: "optimized"' frontend/lib/stores/workspace-v2-store.ts` → ditemukan (PDF-03)
    - `grep -n "toggleLeftPanel" frontend/lib/stores/workspace-v2-store.ts` → ditemukan sebagai action
  </acceptance_criteria>
  <done>
    CSS vars [data-workspace-v2] didefinisikan. Zustand store dibuat dengan leftPanelOpen: false dan viewMode: "optimized".
  </done>
</task>

<task type="auto">
  <name>Task 2: Route workspace-v2 (layout + loading + page) dan WorkspaceV2Shell + Header components</name>
  <read_first>
    - frontend/app/workspace/[job_id]/page.tsx (untuk melihat pola fetch hydration yang sudah ada)
    - frontend/app/workspace/[job_id]/loading.tsx (untuk melihat pola loading skeleton)
    - frontend/components/workspace-v2/ (direktori akan dibuat — periksa apakah sudah ada)
    - .planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md (Section: Layout Contract dan Header Design Contract)
  </read_first>
  <files>
    frontend/app/workspace-v2/[job_id]/layout.tsx,
    frontend/app/workspace-v2/[job_id]/loading.tsx,
    frontend/app/workspace-v2/[job_id]/page.tsx,
    frontend/components/workspace-v2/shell.tsx,
    frontend/components/workspace-v2/header.tsx
  </files>
  <action>
**Buat direktori struktur:**
```
frontend/app/workspace-v2/[job_id]/
frontend/components/workspace-v2/
```

**1. `frontend/app/workspace-v2/[job_id]/layout.tsx`** — import CSS layer react-pdf:
```tsx
import type { Metadata } from "next";

// CSS react-pdf diperlukan untuk text layer rendering (PDF-01)
// Diimport di layout supaya berlaku untuk seluruh workspace-v2 route
import "react-pdf/dist/Page/TextLayer.css";
import "react-pdf/dist/Page/AnnotationLayer.css";

export const metadata: Metadata = {
  title: "Analisis CV | CV Analyzer",
};

export default function WorkspaceV2Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
```

**2. `frontend/app/workspace-v2/[job_id]/loading.tsx`** — route-level skeleton:
```tsx
// WorkspaceSkeleton akan dibuat di Plan 05 — untuk sekarang gunakan div placeholder
export default function WorkspaceV2Loading() {
  return (
    <div
      data-workspace-v2
      className="flex h-screen flex-col items-center justify-center bg-[#111111]"
      aria-busy="true"
      aria-live="polite"
    >
      <div className="animate-pulse text-sm text-[rgba(245,242,216,0.35)]">
        Memuat ruang kerja...
      </div>
    </div>
  );
}
```

**3. `frontend/app/workspace-v2/[job_id]/page.tsx`** — Server Component yang fetch hydration:
```tsx
import { getWorkspaceHydration } from "@/lib/workspace";
import { WorkspaceV2Shell } from "@/components/workspace-v2/shell";

interface WorkspaceV2PageProps {
  params: Promise<{ job_id: string }>;
}

export default async function WorkspaceV2Page({ params }: WorkspaceV2PageProps) {
  const { job_id } = await params;

  // Fetch hydration data — sama seperti workspace v1
  // getJobFileUrl akan di-fetch di Plan 05 setelah file URL endpoint ada
  const hydration = await getWorkspaceHydration(job_id).catch(() => null);

  return <WorkspaceV2Shell jobId={job_id} hydration={hydration} />;
}
```

**4. `frontend/components/workspace-v2/shell.tsx`** — Root layout dengan 3-panel grid:
```tsx
"use client";
import { useEffect } from "react";
import { cn } from "@/lib/utils";
import { useWorkspaceV2Store } from "@/lib/stores/workspace-v2-store";
import { WorkspaceV2Header } from "./header";
import type { WorkspaceHydration } from "@/lib/workspace";

interface WorkspaceV2ShellProps {
  jobId: string;
  hydration: WorkspaceHydration | null;
}

export function WorkspaceV2Shell({ jobId, hydration }: WorkspaceV2ShellProps) {
  const { leftPanelOpen, setJobId, setHydration } = useWorkspaceV2Store();

  // Inisialisasi store dengan job context
  useEffect(() => {
    setJobId(jobId);
    if (hydration) setHydration(hydration);
  }, [jobId, hydration, setJobId, setHydration]);

  return (
    <div
      data-workspace-v2
      className="flex h-screen flex-col overflow-hidden bg-[--ws-bg] text-[--ws-ink]"
    >
      {/* Header — h-12 fixed */}
      <WorkspaceV2Header
        filename={hydration?.file.filename ?? null}
        jobId={jobId}
        jobStatus={hydration?.status}
        className="h-12 flex-none border-b border-[--ws-border-strong]"
      />

      {/* Body — 3-panel grid on desktop, single column on mobile */}
      <div
        className={cn(
          "flex-1 overflow-hidden",
          "grid lg:grid-cols-[290px_minmax(0,1fr)_340px]",
          // Transition grid template ketika left panel toggle
          leftPanelOpen
            ? "lg:grid-cols-[290px_minmax(0,1fr)_340px]"
            : "lg:grid-cols-[0px_minmax(0,1fr)_340px]",
          "transition-[grid-template-columns] duration-200 ease-in-out"
        )}
      >
        {/* Left panel — hidden di mobile, collapsed by default */}
        {/* Placeholder — akan diisi LeftDetailPanel di Plan 05 */}
        <aside
          className={cn(
            "hidden lg:flex flex-col overflow-hidden",
            "bg-[--ws-surface] border-r border-[--ws-border]",
            leftPanelOpen ? "w-[290px]" : "w-0 overflow-hidden opacity-0 pointer-events-none",
            "transition-[width,opacity] duration-200 ease-in-out"
          )}
          aria-hidden={!leftPanelOpen}
        >
          {/* LeftDetailPanel — Plan 05 */}
        </aside>

        {/* Center PDF panel */}
        {/* Placeholder — akan diisi PdfViewerPanel di Plan 05 */}
        <main className="relative flex flex-col overflow-hidden bg-[--ws-bg]">
          {/* PdfViewerPanel + LeftPanelToggle — Plan 05 */}
          <div className="flex flex-1 items-center justify-center">
            <p className="text-sm text-[--ws-ink-ghost]">
              PDF viewer sedang disiapkan...
            </p>
          </div>
        </main>

        {/* Right rail — hidden di mobile */}
        {/* Placeholder — akan diisi RightRailStats di Plan 05 */}
        <aside
          className="hidden lg:flex flex-col bg-[--ws-surface] border-l border-[--ws-border] w-[340px] overflow-y-auto"
        >
          {/* RightRailStats — Plan 05 */}
        </aside>
      </div>
    </div>
  );
}
```

**5. `frontend/components/workspace-v2/header.tsx`** — Minimal h-12 header:
```tsx
"use client";
import { useRouter } from "next/navigation";
import { ChevronLeft } from "lucide-react";
import { cn } from "@/lib/utils";

interface WorkspaceV2HeaderProps {
  filename: string | null;
  jobId: string;
  jobStatus?: "preparing" | "ready" | "failed";
  className?: string;
}

export function WorkspaceV2Header({
  filename,
  jobId,
  jobStatus,
  className,
}: WorkspaceV2HeaderProps) {
  const router = useRouter();

  return (
    <header
      className={cn(
        "flex items-center gap-3 px-4 bg-[--ws-bg]",
        className
      )}
    >
      {/* Tombol Kembali */}
      <button
        onClick={() => router.push(`/results/${jobId}`)}
        className="flex h-8 items-center gap-1 px-2 text-sm font-normal text-[--ws-ink-secondary] transition-colors duration-150 hover:text-[--ws-ink]"
        aria-label="Kembali ke halaman hasil"
      >
        <ChevronLeft className="h-4 w-4" />
        <span>Kembali</span>
      </button>

      {/* Divider */}
      <div className="h-4 w-px bg-[--ws-border-strong]" aria-hidden="true" />

      {/* Filename */}
      <p
        className="min-w-0 flex-1 truncate text-sm font-normal text-[--ws-ink]"
        style={{ maxWidth: "360px" }}
      >
        {filename ?? "Dokumen"}
      </p>

      {/* Job status pill — hanya tampil jika status ready (completed) */}
      {jobStatus === "ready" && (
        <span
          className="flex-none rounded-full bg-[--ws-accent-muted] px-2 py-0.5 text-xs font-bold text-[--ws-accent]"
          aria-label="Status: Selesai"
        >
          Selesai
        </span>
      )}
    </header>
  );
}
```
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/workspace-v2/[job_id]/layout.tsx` ada dan mengandung `import "react-pdf/dist/Page/TextLayer.css"`
    - `frontend/app/workspace-v2/[job_id]/page.tsx` ada
    - `grep -n "WorkspaceV2Shell" frontend/app/workspace-v2/[job_id]/page.tsx` → ditemukan
    - `frontend/components/workspace-v2/shell.tsx` ada
    - `grep -n "data-workspace-v2" frontend/components/workspace-v2/shell.tsx` → ditemukan pada root div
    - `grep -n "lg:grid-cols-\[290px" frontend/components/workspace-v2/shell.tsx` → ditemukan (grid kolom spesifik)
    - `grep -n "leftPanelOpen: false\|leftPanelOpen.*false" frontend/components/workspace-v2/shell.tsx` → TIDAK DIPERLUKAN (state ada di store, bukan hardcoded di shell)
    - `grep -n "hidden lg:flex" frontend/components/workspace-v2/shell.tsx` → ditemukan minimal 2 kali (left + right panels)
    - `frontend/components/workspace-v2/header.tsx` ada
    - `grep -n "Kembali" frontend/components/workspace-v2/header.tsx` → ditemukan (teks tombol back)
    - `grep -n "ChevronLeft" frontend/components/workspace-v2/header.tsx` → ditemukan (icon dari lucide-react)
    - `grep -n "Selesai" frontend/components/workspace-v2/header.tsx` → ditemukan (status badge teks)
    - `grep -n "Dokumen" frontend/components/workspace-v2/header.tsx` → ditemukan (filename fallback)
    - `npx tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>
    Route /workspace-v2/[job_id] terdaftar. WorkspaceV2Shell merender 3-panel layout dengan left/right panels hidden di mobile. WorkspaceV2Header menampilkan Kembali + filename + badge Selesai. Route bisa dibuka di browser (menampilkan skeleton/placeholder content).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → page.tsx | job_id dari URL — dipass ke getWorkspaceHydration (yang fetch dari backend) |
| Zustand store → components | State di client-side only — tidak ada server state exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | page.tsx — hydration fetch error | mitigate | `getWorkspaceHydration().catch(() => null)` — null hydration ditampilkan sebagai empty state, bukan crash |
| T-13-03-02 | Tampering | CSS vars scoping | mitigate | Vars scoped ke `[data-workspace-v2]` — tidak bisa diakses dari landing page; tidak override global :root |
| T-13-03-03 | Spoofing | Zustand store initialization | accept | Store hanya diinisialisasi dari server-fetched hydration; tidak ada user-controlled input path ke store state |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit
```
Harus exit 0.

```bash
grep -rn "data-workspace-v2" frontend/components/workspace-v2/shell.tsx
grep -n "\-\-ws-bg" frontend/app/globals.css
grep -n "leftPanelOpen: false" frontend/lib/stores/workspace-v2-store.ts
```
Semua harus ditemukan.
</verification>

<success_criteria>
- CSS vars [data-workspace-v2] ada di globals.css dengan semua 15 token
- Zustand store dengan leftPanelOpen: false dan viewMode: "optimized"
- Route /workspace-v2/[job_id] ada dengan layout.tsx (react-pdf CSS imports), loading.tsx, page.tsx
- WorkspaceV2Shell — 3-panel grid dengan transition, mobile panels hidden
- WorkspaceV2Header — h-12, Kembali button, filename truncate, Selesai badge
- TypeScript clean (tsc --noEmit exit 0)
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/13-pdf-first-workspace-shell/13-03-SUMMARY.md` dengan:
- Konfirmasi route /workspace-v2/[job_id] bisa diakses (test manual atau curl)
- Screenshot atau deskripsi tampilan shell (struktur 3 kolom terlihat)
- Apakah ada TypeScript warning/error saat tsc --noEmit
- CSS vars yang berhasil didefinisikan (list token names)
</output>
