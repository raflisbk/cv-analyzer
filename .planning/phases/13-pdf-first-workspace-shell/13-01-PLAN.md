---
phase: 13-pdf-first-workspace-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/workspace-utils.ts
  - frontend/components/workspace/canvas/canvas-editor.tsx
  - frontend/components/workspace/canvas/section-block.tsx
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - LAYOUT-02

must_haves:
  truths:
    - "frontend/lib/workspace-utils.ts exists dan mengekspor plainTextToTiptapDoc, normalizeSuggestion, buildInitialSections"
    - "canvas-editor.tsx dan section-block.tsx tidak lagi mendefinisikan ketiga fungsi tersebut secara lokal — semua diimport dari @/lib/workspace-utils"
    - "npm install berhasil tanpa error — react-pdf, yjs, y-indexeddb, zustand, @floating-ui/react tercantum di package.json"
    - "TypeScript tidak error setelah refactor (tsc --noEmit berjalan clean)"
  artifacts:
    - path: "frontend/lib/workspace-utils.ts"
      provides: "Shared utility functions untuk workspace-v2 dan canvas"
      exports: ["plainTextToTiptapDoc", "normalizeSuggestion", "buildInitialSections"]
    - path: "frontend/package.json"
      provides: "Package dependencies"
      contains: "react-pdf"
  key_links:
    - from: "frontend/components/workspace/canvas/canvas-editor.tsx"
      to: "frontend/lib/workspace-utils.ts"
      via: "named import"
      pattern: "from \"@/lib/workspace-utils\""
    - from: "frontend/components/workspace/canvas/section-block.tsx"
      to: "frontend/lib/workspace-utils.ts"
      via: "named import (plainTextToTiptapDoc)"
      pattern: "from \"@/lib/workspace-utils\""
---

<objective>
Ekstraksi tiga utility function dari canvas-editor.tsx dan section-block.tsx ke dalam `frontend/lib/workspace-utils.ts` yang bisa dibagikan antara workspace v1 (Phase 12) dan workspace-v2 (Phase 13+). Setelah itu install semua package frontend yang dibutuhkan Phase 13.

Purpose: Utility extraction harus dilakukan SEBELUM workspace-v2 dibangun supaya tidak ada import langsung dari canvas/ directory ke workspace-v2 components. Ini menjaga isolasi total antara dua workspace.

Output: `frontend/lib/workspace-utils.ts` dengan 3 fungsi yang bisa di-reuse; canvas-editor.tsx dan section-block.tsx mengimport dari sana; 5 package baru terinstal.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-pdf-first-workspace-shell/13-CONTEXT.md
@.planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md
@frontend/components/workspace/canvas/canvas-editor.tsx
@frontend/components/workspace/canvas/section-block.tsx
@frontend/lib/workspace.ts
@frontend/lib/types.ts

<interfaces>
<!-- Fungsi yang diekstrak — verifikasi signature sebelum copy -->
<!-- Dari canvas-editor.tsx baris 24–54: -->
```typescript
// normalizeSuggestion — line 24-36
function normalizeSuggestion(raw: Record<string, unknown>): SuggestionItem {
  return {
    priority: (raw.priority as SuggestionItem["priority"]) ?? "quick_win",
    text: (raw.text as string) ?? "",
    type: (raw.type as SuggestionItem["type"]) ?? "action_verb",
    originalText: (raw.originalText as string | undefined) ?? (raw.original_text as string | undefined),
    afterText: (raw.afterText as string | undefined) ?? (raw.after_text as string | undefined),
  };
}

// buildInitialSections — line 38-54
function buildInitialSections(
  sections: WorkspaceHydration["document"]["sections"],
  draftContent?: Record<string, JSONContent> | null
): SectionState[] { ... }
```

<!-- Dari section-block.tsx baris 38-47: -->
```typescript
// plainTextToTiptapDoc — exported function
export function plainTextToTiptapDoc(text: string): JSONContent {
  const lines = text.split("\n");
  return {
    type: "doc",
    content: lines.map((line) => ({
      type: "paragraph",
      content: line.trim() ? [{ type: "text", text: line }] : [],
    })),
  };
}
```

<!-- Types tersedia di frontend/lib/types.ts:
  SuggestionItem { priority, text, type, originalText?, afterText? }
  SectionResult { type, text, entities }
-->
<!-- JSONContent dari @tiptap/core -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ekstrak utility functions ke workspace-utils.ts dan update imports di canvas files</name>
  <read_first>
    - frontend/components/workspace/canvas/canvas-editor.tsx (baris 1-60 untuk normalizeSuggestion + buildInitialSections)
    - frontend/components/workspace/canvas/section-block.tsx (baris 1-50 untuk plainTextToTiptapDoc + SpacingValue type)
    - frontend/lib/types.ts (untuk SuggestionItem interface yang diimport)
    - frontend/lib/workspace.ts (untuk WorkspaceHydration type)
  </read_first>
  <files>
    frontend/lib/workspace-utils.ts,
    frontend/components/workspace/canvas/canvas-editor.tsx,
    frontend/components/workspace/canvas/section-block.tsx
  </files>
  <action>
**Buat file baru `frontend/lib/workspace-utils.ts`** dengan konten berikut (copy verbatim dari source, jangan ubah logic):

```typescript
/**
 * Shared workspace utility functions.
 * Diekstrak dari canvas-editor.tsx dan section-block.tsx pada Phase 13
 * agar dapat dibagikan antara workspace-v1 (canvas) dan workspace-v2.
 *
 * PENTING: Jangan ubah behavior function ini — canvas editor bergantung padanya.
 */
import type { JSONContent } from "@tiptap/core";
import type { WorkspaceHydration } from "@/lib/workspace";
import type { SuggestionItem } from "@/lib/types";

export type SpacingValue = "compact" | "normal" | "spacious";

export interface SectionState {
  type: string;
  json: JSONContent;
  spacing: SpacingValue;
}

/** Konversi plain text (dengan newlines) ke Tiptap-compatible doc JSONContent */
export function plainTextToTiptapDoc(text: string): JSONContent {
  const lines = text.split("\n");
  return {
    type: "doc",
    content: lines.map((line) => ({
      type: "paragraph",
      content: line.trim() ? [{ type: "text", text: line }] : [],
    })),
  };
}

/** Normalisasi raw API suggestion item — backend mengirim snake_case, frontend mengharapkan camelCase */
export function normalizeSuggestion(raw: Record<string, unknown>): SuggestionItem {
  return {
    priority: (raw.priority as SuggestionItem["priority"]) ?? "quick_win",
    text: (raw.text as string) ?? "",
    type: (raw.type as SuggestionItem["type"]) ?? "action_verb",
    originalText:
      (raw.originalText as string | undefined) ??
      (raw.original_text as string | undefined),
    afterText:
      (raw.afterText as string | undefined) ??
      (raw.after_text as string | undefined),
  };
}

/** Bangun initial sections state dari workspace hydration data, merge duplicate section types */
export function buildInitialSections(
  sections: WorkspaceHydration["document"]["sections"],
  draftContent?: Record<string, JSONContent> | null
): SectionState[] {
  // Deduplicate by type — merge text of duplicate sections (NLP may emit multiple blocks per type)
  const merged = new Map<string, string>();
  for (const section of sections) {
    const existing = merged.get(section.type);
    merged.set(section.type, existing ? `${existing}\n${section.text}` : section.text);
  }

  return Array.from(merged.entries()).map(([type, text]) => ({
    type,
    json: draftContent?.[type] ?? plainTextToTiptapDoc(text),
    spacing: "normal" as SpacingValue,
  }));
}
```

**Update `frontend/components/workspace/canvas/canvas-editor.tsx`:**
1. Tambahkan import: `import { normalizeSuggestion, buildInitialSections, type SectionState, type SpacingValue } from "@/lib/workspace-utils";`
2. Hapus definisi lokal `normalizeSuggestion` (baris 24-36) dan `buildInitialSections` (baris 38-54)
3. Hapus lokal `SpacingValue` type dan `SectionState` interface (sudah ada di workspace-utils)
4. Import `{ plainTextToTiptapDoc }` dari workspace-utils juga (tidak dari section-block lagi) — UPDATE: periksa apakah canvas-editor.tsx mengimport plainTextToTiptapDoc dari section-block. Jika ya, pindahkan ke workspace-utils import. Jika sudah via buildInitialSections, biarkan saja.

**Update `frontend/components/workspace/canvas/section-block.tsx`:**
1. Tambahkan re-export: `export { plainTextToTiptapDoc } from "@/lib/workspace-utils";` DAN juga import `plainTextToTiptapDoc` dari workspace-utils untuk digunakan di internal file
2. ATAU: ganti definisi lokal `export function plainTextToTiptapDoc` dengan import dari workspace-utils dan re-export: 
   ```typescript
   export { plainTextToTiptapDoc } from "@/lib/workspace-utils";
   ```
3. Pastikan `SpacingValue` type di section-block.tsx diimport dari workspace-utils (atau redefined locally — tidak masalah karena identik, tapi lebih baik import untuk konsistensi)

**Setelah refactor, canvas-editor.tsx tidak boleh mendefinisikan normalizeSuggestion, buildInitialSections, SpacingValue, atau SectionState secara lokal.**
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function plainTextToTiptapDoc" frontend/lib/workspace-utils.ts` → ditemukan di baris yang benar
    - `grep -n "export function normalizeSuggestion" frontend/lib/workspace-utils.ts` → ditemukan
    - `grep -n "export function buildInitialSections" frontend/lib/workspace-utils.ts` → ditemukan
    - `grep -n "from \"@/lib/workspace-utils\"" frontend/components/workspace/canvas/canvas-editor.tsx` → ditemukan
    - `grep -n "function normalizeSuggestion" frontend/components/workspace/canvas/canvas-editor.tsx` → TIDAK ditemukan (fungsi sudah dipindah)
    - `grep -n "function buildInitialSections" frontend/components/workspace/canvas/canvas-editor.tsx` → TIDAK ditemukan
    - `grep -n "workspace-utils" frontend/components/workspace/canvas/section-block.tsx` → ditemukan (re-export atau import)
    - `npx tsc --noEmit` dalam folder frontend → exit code 0 (tidak ada TypeScript error)
  </acceptance_criteria>
  <done>
    frontend/lib/workspace-utils.ts ada dengan 3 fungsi yang diekspor. canvas-editor.tsx dan section-block.tsx mengimport dari workspace-utils. TypeScript berjalan clean tanpa error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install frontend packages (react-pdf, yjs, y-indexeddb, zustand, @floating-ui/react)</name>
  <read_first>
    - frontend/package.json (untuk melihat versi React/Next.js yang ada dan menghindari konflik)
  </read_first>
  <files>
    frontend/package.json,
    frontend/package-lock.json
  </files>
  <action>
Jalankan install dari direktori `frontend/`:

```bash
cd frontend
npm install react-pdf yjs y-indexeddb zustand @floating-ui/react
```

Versi yang di-resolve (sesuai npm registry per 2026-04-14):
- `react-pdf` → `10.4.1` (bundles pdfjs-dist 5.4.296 — jangan install pdfjs-dist terpisah)
- `yjs` → `13.6.30`
- `y-indexeddb` → `9.0.12`
- `zustand` → `5.0.12`
- `@floating-ui/react` → `0.27.19`

**Setelah install, verifikasi tidak ada peer dependency conflict di output npm:**
- react-pdf v10.4.1 mendukung React 19 (`"react": "^16.8.0 || ^17.0.0 || ^18.0.0 || ^19.0.0"`) ✓
- zustand 5.0.12 mendukung React 19 ✓
- Jika ada warning "WARN" tentang peer deps, check apakah ini React 19 warning yang bisa diabaikan. Jangan gunakan `--legacy-peer-deps` kecuali npm menolak install.

**JANGAN** install `pdfjs-dist` secara terpisah — react-pdf v10 sudah bundling versinya sendiri dan akan conflict jika ada dua versi.
  </action>
  <verify>
    <automated>
      cd frontend && node -e "const pkg = require('./package.json'); ['react-pdf','yjs','y-indexeddb','zustand','@floating-ui/react'].forEach(p => console.log(p, pkg.dependencies[p] || 'MISSING'))"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep '"react-pdf"' frontend/package.json` → ditemukan dengan versi ^10.x.x
    - `grep '"yjs"' frontend/package.json` → ditemukan dengan versi ^13.x.x
    - `grep '"y-indexeddb"' frontend/package.json` → ditemukan dengan versi ^9.x.x
    - `grep '"zustand"' frontend/package.json` → ditemukan dengan versi ^5.x.x
    - `grep '"@floating-ui/react"' frontend/package.json` → ditemukan
    - `frontend/node_modules/react-pdf/package.json` ada (direktori terinstall)
    - `frontend/node_modules/zustand/package.json` ada
    - npm install exit dengan code 0 (tidak ada error fatal)
  </acceptance_criteria>
  <done>
    Semua 5 package terinstall di frontend/node_modules. package.json mencantumkan keseluruhan 5 dependency baru. Tidak ada peer dependency error fatal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local | Package install dari npm — versi yang diinstall harus match dengan yang sudah diverifikasi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | canvas-editor.tsx refactor | mitigate | Baca file source sebelum edit; jalankan tsc --noEmit untuk konfirmasi tidak ada regression; jangan ubah logic fungsi yang diekstrak |
| T-13-01-02 | Denial of Service | npm install | accept | react-pdf v10 bundles pdfjs-dist; tidak ada versi conflict; install dari npm official registry |
</threat_model>

<verification>
Jalankan di root cv-analyzer:
```bash
cd frontend && npx tsc --noEmit
```
Harus exit 0. Kemudian:
```bash
grep -c "export function" frontend/lib/workspace-utils.ts
```
Harus mengembalikan `3` (tiga fungsi diekspor).
</verification>

<success_criteria>
- frontend/lib/workspace-utils.ts ada dengan plainTextToTiptapDoc, normalizeSuggestion, buildInitialSections diekspor
- canvas-editor.tsx mengimport dari @/lib/workspace-utils (bukan mendefinisikan lokal)
- section-block.tsx tidak menduplikasi plainTextToTiptapDoc (re-export atau import dari workspace-utils)
- 5 package baru tercantum di frontend/package.json
- npx tsc --noEmit berjalan clean setelah refactor
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/13-pdf-first-workspace-shell/13-01-SUMMARY.md` dengan:
- Fungsi apa yang diekstrak dan dari mana
- Apakah ada perubahan signature saat refactor
- Versi package yang terinstall (output dari package.json)
- Hasil tsc --noEmit (pass/fail)
</output>
