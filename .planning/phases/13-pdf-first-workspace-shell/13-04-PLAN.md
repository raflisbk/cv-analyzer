---
phase: 13-pdf-first-workspace-shell
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/components/workspace-v2/pdf-viewer-inner.tsx
  - frontend/components/workspace-v2/pdf-viewer.tsx
  - frontend/components/workspace-v2/pdf-viewer-panel.tsx
  - frontend/components/workspace-v2/pdf-viewer-skeleton.tsx
  - frontend/components/workspace-v2/pdf-viewer-error.tsx
autonomous: true
requirements:
  - PDF-01

must_haves:
  truths:
    - "PdfViewerInner merender react-pdf <Document> + <Page> dengan renderTextLayer={true} dan worker dikonfigurasi"
    - "PdfViewer adalah dynamic() wrapper dengan ssr: false yang menampilkan PdfViewerSkeleton saat loading"
    - "PdfViewerPanel menggunakan ResizeObserver untuk meneruskan containerWidth ke PdfViewer"
    - "PDF paper memiliki bg-[--ws-paper] (#FFFDF4), shadow 0 8px 48px rgba(0,0,0,0.65), rounded-2xl"
    - "PdfViewerSkeleton menampilkan shimmer lines animate-pulse dalam paper shape"
    - "PdfViewerError menampilkan role='alert', heading 'Dokumen Tidak Tersedia', tombol 'Muat Ulang'"
    - "Worker path menggunakan string statis — BUKAN new URL(..., import.meta.url) untuk menghindari Next.js webpack issue"
  artifacts:
    - path: "frontend/components/workspace-v2/pdf-viewer-inner.tsx"
      provides: "react-pdf client component dengan Document+Page+worker"
      contains: "renderTextLayer={true}"
    - path: "frontend/components/workspace-v2/pdf-viewer.tsx"
      provides: "SSR-safe dynamic wrapper"
      contains: "ssr: false"
    - path: "frontend/components/workspace-v2/pdf-viewer-panel.tsx"
      provides: "Container dengan ResizeObserver"
      contains: "ResizeObserver"
    - path: "frontend/components/workspace-v2/pdf-viewer-skeleton.tsx"
      provides: "Loading skeleton animasi"
      contains: "animate-pulse"
    - path: "frontend/components/workspace-v2/pdf-viewer-error.tsx"
      provides: "Error state dengan retry"
      contains: "role=\"alert\""
  key_links:
    - from: "frontend/components/workspace-v2/pdf-viewer-panel.tsx"
      to: "frontend/components/workspace-v2/pdf-viewer.tsx"
      via: "render PdfViewer dengan containerWidth prop"
      pattern: "containerWidth"
    - from: "frontend/components/workspace-v2/pdf-viewer.tsx"
      to: "frontend/components/workspace-v2/pdf-viewer-inner.tsx"
      via: "dynamic import ssr:false"
      pattern: "pdf-viewer-inner"
---

<objective>
Bangun stack PDF viewer lengkap untuk workspace-v2: PdfViewerInner (react-pdf), PdfViewer (SSR wrapper), PdfViewerPanel (ResizeObserver container), PdfViewerSkeleton, dan PdfViewerError.

Purpose: Setelah plan ini selesai, stack PDF viewer bisa dirender di dalam WorkspaceV2Shell (Plan 03). PDF bisa dimuat dari presigned URL, merender dengan text layer aktif (dibutuhkan untuk annotation spike Plan 06), dan menampilkan skeleton/error states yang sesuai UI-SPEC.

Output: 5 component files yang membentuk PDF viewer stack yang complete dan SSR-safe.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-first-workspace-shell/13-CONTEXT.md
@.planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md
@.planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md

<interfaces>
<!-- react-pdf v10.4.1 API (package dari Plan 01) -->
```typescript
import { Document, Page, pdfjs } from "react-pdf";

// Worker setup — gunakan string statis (bukan new URL(..., import.meta.url))
// ALASAN: Next.js webpack akan mencoba resolve URL(..., import.meta.url) pada build time
// Ini menyebabkan error di App Router. String path statis aman.
pdfjs.GlobalWorkerOptions.workerSrc = "/pdf.worker.min.mjs";
// File pdf.worker.min.mjs harus di-copy ke frontend/public/ dari node_modules

// Document props:
// file: string (URL) | { url: string } | { data: ArrayBuffer }
// onLoadSuccess: ({ numPages: number }) => void
// loading: ReactNode
// error: ReactNode

// Page props:
// pageNumber: number (1-indexed)
// width: number (dari ResizeObserver)
// renderTextLayer: boolean
// renderAnnotationLayer: boolean
// onLoadSuccess: (page: PDFPageProxy) => void  ← untuk annotation spike
```

<!-- ResizeObserver pattern dari RESEARCH.md Section 1 -->
```typescript
const containerRef = useRef<HTMLDivElement>(null);
const [containerWidth, setContainerWidth] = useState<number>(0);

useEffect(() => {
  if (!containerRef.current) return;
  const observer = new ResizeObserver((entries) => {
    const width = entries[0]?.contentRect.width ?? 0;
    setContainerWidth(Math.floor(width));
  });
  observer.observe(containerRef.current);
  return () => observer.disconnect();
}, []);
// Render PdfViewer hanya jika containerWidth > 0 (menghindari zero-width flash)
```

<!-- UI-SPEC values yang wajib digunakan: -->
<!--
  PDF paper: bg-[#FFFDF4] atau bg-[--ws-paper]
  Paper shadow: shadow-[0_8px_48px_rgba(0,0,0,0.65)]
  Paper border-radius: rounded-2xl
  Paper max-width: max-w-[860px] mx-auto
  Paper min-height: min-h-[1080px]
  Paper border: border border-[rgba(202,255,67,0.08)]
  Outer container (dark shell): bg-[--ws-bg] p-6 overflow-y-auto
  Toolbar: bg-[rgba(17,17,17,0.04)] text-xs font-bold text-[rgba(17,17,17,0.6)]
  Skeleton shimmer: bg-[rgba(17,17,17,0.07)] animate-pulse rounded
  Error icon: lucide AlertTriangle 32px text-[--ws-destructive]
  Error heading: text-[15px] font-bold text-[rgba(17,17,17,0.9)]
  Error button: bg-[#111111] text-[#F5F2D8] text-sm font-bold px-4 py-2 rounded-lg
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PdfViewerInner, PdfViewer (dynamic wrapper), dan PdfViewerPanel (ResizeObserver)</name>
  <read_first>
    - frontend/node_modules/react-pdf/package.json (untuk konfirmasi versi dan bundled worker path)
    - .planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md (Section 1 — Worker Setup dan SSR Suppression)
    - .planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md (Section 4 — PDF Viewer Panel Design Contract)
  </read_first>
  <files>
    frontend/components/workspace-v2/pdf-viewer-inner.tsx,
    frontend/components/workspace-v2/pdf-viewer.tsx,
    frontend/components/workspace-v2/pdf-viewer-panel.tsx,
    frontend/public/pdf.worker.min.mjs
  </files>
  <action>
**Step 0: Copy worker file ke public/**

react-pdf v10 bundling pdfjs-dist. Worker file ada di:
`frontend/node_modules/pdfjs-dist/build/pdf.worker.min.mjs`

Copy ke `frontend/public/pdf.worker.min.mjs`:
```bash
cp frontend/node_modules/pdfjs-dist/build/pdf.worker.min.mjs frontend/public/pdf.worker.min.mjs
```
Atau di Windows PowerShell:
```powershell
Copy-Item "frontend\node_modules\pdfjs-dist\build\pdf.worker.min.mjs" "frontend\public\pdf.worker.min.mjs"
```

Jika file tidak ada di path itu, coba:
```bash
find frontend/node_modules/pdfjs-dist -name "pdf.worker.min.mjs" 2>/dev/null
```

**1. `frontend/components/workspace-v2/pdf-viewer-inner.tsx`** — react-pdf client component:
```tsx
"use client";
/**
 * PdfViewerInner — react-pdf Document+Page component.
 * Dimuat hanya via dynamic() dengan ssr: false (PDF-01, LAYOUT-02).
 * Worker menggunakan path statis ke /pdf.worker.min.mjs di public/ folder.
 * JANGAN gunakan new URL(..., import.meta.url) — crash di Next.js App Router webpack.
 */
import { useState, useCallback } from "react";
import { Document, Page, pdfjs } from "react-pdf";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { PdfViewerSkeleton } from "./pdf-viewer-skeleton";
import { PdfViewerError } from "./pdf-viewer-error";

// Worker setup — path statis ke /public/pdf.worker.min.mjs (PDF-01)
// Diset di module scope (aman karena file ini hanya diload client-side via dynamic import)
pdfjs.GlobalWorkerOptions.workerSrc = "/pdf.worker.min.mjs";

interface PdfViewerInnerProps {
  url: string;
  containerWidth: number;
  /** Callback untuk annotation spike — dipanggil dengan PDFPageProxy setelah page load */
  onPageLoadSuccess?: (page: unknown) => void;
}

export default function PdfViewerInner({
  url,
  containerWidth,
  onPageLoadSuccess,
}: PdfViewerInnerProps) {
  const [numPages, setNumPages] = useState<number>(0);
  const [currentPage, setCurrentPage] = useState<number>(1);
  const [loadError, setLoadError] = useState<boolean>(false);

  const handleDocumentLoadSuccess = useCallback(
    ({ numPages }: { numPages: number }) => {
      setNumPages(numPages);
      setLoadError(false);
    },
    []
  );

  const handleDocumentLoadError = useCallback(() => {
    setLoadError(true);
  }, []);

  const handleRetry = useCallback(() => {
    setLoadError(false);
    setCurrentPage(1);
  }, []);

  if (loadError) {
    return <PdfViewerError onRetry={handleRetry} errorType="generic" />;
  }

  return (
    <div className="flex flex-col">
      {/* PDF Toolbar — compact strip di atas document */}
      {numPages > 0 && (
        <div className="flex items-center justify-between px-4 py-2 bg-[rgba(17,17,17,0.04)]">
          <span className="text-xs font-bold text-[rgba(17,17,17,0.6)]">
            Halaman {currentPage} / {numPages}
          </span>
          <div className="flex items-center gap-1">
            <button
              onClick={() => setCurrentPage((p) => Math.max(1, p - 1))}
              disabled={currentPage <= 1}
              aria-label="Sebelumnya"
              className="flex h-7 w-7 items-center justify-center rounded text-xs text-[rgba(17,17,17,0.6)] hover:bg-[rgba(17,17,17,0.06)] disabled:opacity-30"
            >
              <ChevronLeft className="h-3.5 w-3.5" />
            </button>
            <button
              onClick={() => setCurrentPage((p) => Math.min(numPages, p + 1))}
              disabled={currentPage >= numPages}
              aria-label="Berikutnya"
              className="flex h-7 w-7 items-center justify-center rounded text-xs text-[rgba(17,17,17,0.6)] hover:bg-[rgba(17,17,17,0.06)] disabled:opacity-30"
            >
              <ChevronRight className="h-3.5 w-3.5" />
            </button>
          </div>
        </div>
      )}

      {/* react-pdf Document */}
      <Document
        file={url}
        onLoadSuccess={handleDocumentLoadSuccess}
        onLoadError={handleDocumentLoadError}
        loading={<PdfViewerSkeleton />}
      >
        <Page
          pageNumber={currentPage}
          width={containerWidth}
          renderTextLayer={true}
          renderAnnotationLayer={false}
          onLoadSuccess={onPageLoadSuccess}
        />
      </Document>
    </div>
  );
}
```

**2. `frontend/components/workspace-v2/pdf-viewer.tsx`** — SSR-safe dynamic wrapper:
```tsx
/**
 * PdfViewer — SSR-safe wrapper untuk PdfViewerInner.
 * Menggunakan Next.js dynamic() dengan ssr: false supaya react-pdf tidak crash di server.
 * (LAYOUT-02, PDF-01)
 */
import dynamic from "next/dynamic";
import { PdfViewerSkeleton } from "./pdf-viewer-skeleton";

const PdfViewerInner = dynamic(
  () => import("./pdf-viewer-inner"),
  {
    ssr: false,
    loading: () => <PdfViewerSkeleton />,
  }
);

interface PdfViewerProps {
  url: string | null;
  containerWidth: number;
  onPageLoadSuccess?: (page: unknown) => void;
}

export function PdfViewer({ url, containerWidth, onPageLoadSuccess }: PdfViewerProps) {
  if (!url) {
    return (
      <div
        className="flex min-h-[1080px] max-w-[860px] mx-auto items-center justify-center rounded-2xl bg-[#FFFDF4] shadow-[0_8px_48px_rgba(0,0,0,0.65)]"
        role="alert"
      >
        <p className="text-sm text-[rgba(17,17,17,0.55)] text-center max-w-[280px]">
          PDF tidak tersedia. File mungkin belum diproses.
        </p>
      </div>
    );
  }

  if (containerWidth === 0) {
    return <PdfViewerSkeleton />;
  }

  return (
    <PdfViewerInner
      url={url}
      containerWidth={containerWidth}
      onPageLoadSuccess={onPageLoadSuccess}
    />
  );
}
```

**3. `frontend/components/workspace-v2/pdf-viewer-panel.tsx`** — Container dengan ResizeObserver:
```tsx
"use client";
/**
 * PdfViewerPanel — outer container untuk PDF viewer.
 * ResizeObserver mengukur container width dan meneruskan ke PdfViewer.
 * Menampilkan PDF paper (cream #FFFDF4) floating di dark shell (#111111).
 */
import { useRef, useState, useEffect } from "react";
import { PdfViewer } from "./pdf-viewer";

interface PdfViewerPanelProps {
  pdfUrl: string | null;
  onPageLoadSuccess?: (page: unknown) => void;
}

export function PdfViewerPanel({ pdfUrl, onPageLoadSuccess }: PdfViewerPanelProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const [containerWidth, setContainerWidth] = useState<number>(0);

  useEffect(() => {
    if (!containerRef.current) return;
    const observer = new ResizeObserver((entries) => {
      const width = entries[0]?.contentRect.width ?? 0;
      setContainerWidth(Math.floor(width));
    });
    observer.observe(containerRef.current);
    return () => observer.disconnect();
  }, []);

  return (
    // Dark shell outer — bg #111111, padding p-6, scroll the PDF
    <div className="flex-1 overflow-y-auto bg-[--ws-bg] p-6">
      {/* Paper container — cream #FFFDF4, max-w 860px, shadow, rounded */}
      <div
        ref={containerRef}
        className="
          mx-auto max-w-[860px] min-h-[1080px]
          rounded-2xl
          bg-[#FFFDF4]
          border border-[rgba(202,255,67,0.08)]
          shadow-[0_8px_48px_rgba(0,0,0,0.65)]
          overflow-hidden
        "
        aria-label="Dokumen PDF"
      >
        <PdfViewer
          url={pdfUrl}
          containerWidth={containerWidth}
          onPageLoadSuccess={onPageLoadSuccess}
        />
      </div>
      {/* Loading copy dibawah paper */}
      {!pdfUrl && (
        <p className="mt-4 text-center text-sm text-[--ws-ink-secondary]">
          Memuat dokumen...
        </p>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | grep -E "pdf-viewer|error" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/public/pdf.worker.min.mjs` ada (file berhasil di-copy)
    - `grep -n "renderTextLayer={true}" frontend/components/workspace-v2/pdf-viewer-inner.tsx` → ditemukan
    - `grep -n "renderAnnotationLayer={false}" frontend/components/workspace-v2/pdf-viewer-inner.tsx` → ditemukan
    - `grep -n 'workerSrc = "/pdf.worker.min.mjs"' frontend/components/workspace-v2/pdf-viewer-inner.tsx` → ditemukan (string statis, BUKAN new URL)
    - `grep -n "new URL.*import.meta" frontend/components/workspace-v2/pdf-viewer-inner.tsx` → TIDAK ditemukan (pola yang dihindari)
    - `grep -n "ssr: false" frontend/components/workspace-v2/pdf-viewer.tsx` → ditemukan
    - `grep -n "pdf-viewer-inner" frontend/components/workspace-v2/pdf-viewer.tsx` → ditemukan dalam dynamic import
    - `grep -n "ResizeObserver" frontend/components/workspace-v2/pdf-viewer-panel.tsx` → ditemukan
    - `grep -n "containerWidth" frontend/components/workspace-v2/pdf-viewer-panel.tsx` → ditemukan (state + pass to PdfViewer)
    - `grep -n "shadow-\[0_8px_48px_rgba" frontend/components/workspace-v2/pdf-viewer-panel.tsx` → ditemukan (paper shadow)
    - `grep -n "bg-\[#FFFDF4\]\|bg-\[--ws-paper\]" frontend/components/workspace-v2/pdf-viewer-panel.tsx` → ditemukan (paper color)
    - `grep -n "rounded-2xl" frontend/components/workspace-v2/pdf-viewer-panel.tsx` → ditemukan
  </acceptance_criteria>
  <done>
    pdf-viewer-inner.tsx dengan react-pdf + worker statis + renderTextLayer=true. pdf-viewer.tsx dynamic wrapper ssr:false. pdf-viewer-panel.tsx dengan ResizeObserver dan paper styling. Worker file di public/. TypeScript clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: PdfViewerSkeleton (animate-pulse shimmer) dan PdfViewerError (role=alert + retry)</name>
  <read_first>
    - .planning/phases/13-pdf-first-workspace-shell/13-UI-SPEC.md (Section 4 — Loading State dan Error State)
    - frontend/components/workspace-v2/pdf-viewer-inner.tsx (untuk melihat errorType prop yang diexpect)
  </read_first>
  <files>
    frontend/components/workspace-v2/pdf-viewer-skeleton.tsx,
    frontend/components/workspace-v2/pdf-viewer-error.tsx
  </files>
  <action>
**1. `frontend/components/workspace-v2/pdf-viewer-skeleton.tsx`** — animasi shimmer A4 paper:
```tsx
/**
 * PdfViewerSkeleton — loading state untuk PDF viewer.
 * Shimmer bars animate-pulse dalam paper shape (cream bg, rounded, shadow).
 * (UI-SPEC Section 4 — Loading State)
 */
export function PdfViewerSkeleton() {
  return (
    <div className="flex flex-col items-center">
      {/* Paper skeleton — sama dimensi dengan real PDF paper */}
      <div
        className="
          mx-auto w-full max-w-[860px] min-h-[1080px]
          rounded-2xl bg-[#FFFDF4]
          border border-[rgba(202,255,67,0.08)]
          shadow-[0_8px_48px_rgba(0,0,0,0.65)]
          p-8
        "
        aria-busy="true"
        aria-live="polite"
      >
        {/* Name/title shimmer (40% width) */}
        <div className="mb-6 h-6 w-[40%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />

        {/* Body lines group 1 */}
        <div className="mb-2 h-4 w-[80%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[65%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[88%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-6 h-4 w-[32%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />

        {/* Body lines group 2 */}
        <div className="mb-2 h-4 w-[64%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[88%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[75%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-6 h-4 w-[50%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />

        {/* Body lines group 3 */}
        <div className="mb-2 h-4 w-[70%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[88%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
        <div className="mb-2 h-4 w-[55%] rounded bg-[rgba(17,17,17,0.07)] animate-pulse" />
      </div>
      {/* Copy di bawah paper */}
      <p className="mt-4 text-center text-sm text-[rgba(245,242,216,0.65)]">
        Memuat dokumen...
      </p>
    </div>
  );
}
```

**2. `frontend/components/workspace-v2/pdf-viewer-error.tsx`** — error state dengan retry:
```tsx
/**
 * PdfViewerError — error state untuk PDF viewer.
 * role="alert" untuk accessibility. Tombol "Muat Ulang" untuk retry.
 * (UI-SPEC Section 4 — Error State)
 */
import { AlertTriangle } from "lucide-react";

type ErrorType = "no-file" | "network" | "generic";

interface PdfViewerErrorProps {
  onRetry?: () => void;
  errorType?: ErrorType;
}

const ERROR_MESSAGES: Record<ErrorType, string> = {
  "no-file": "PDF tidak tersedia. File mungkin belum diproses.",
  "network": "Dokumen tidak dapat dimuat. Periksa koneksi internet.",
  "generic": "Terjadi kesalahan saat memuat dokumen. Coba muat ulang halaman.",
};

export function PdfViewerError({
  onRetry,
  errorType = "generic",
}: PdfViewerErrorProps) {
  return (
    <div
      role="alert"
      className="
        flex min-h-[1080px] max-w-[860px] mx-auto
        flex-col items-center justify-center gap-4
        rounded-2xl bg-[#FFFDF4]
        border border-[rgba(202,255,67,0.08)]
        shadow-[0_8px_48px_rgba(0,0,0,0.65)]
        p-8
      "
    >
      {/* Error icon */}
      <AlertTriangle
        className="h-8 w-8 text-[--ws-destructive]"
        aria-hidden="true"
      />

      {/* Error heading */}
      <h2 className="text-[15px] font-bold text-[rgba(17,17,17,0.9)] text-center">
        Dokumen Tidak Tersedia
      </h2>

      {/* Error body */}
      <p className="max-w-[280px] text-center text-sm text-[rgba(17,17,17,0.55)]">
        {ERROR_MESSAGES[errorType]}
      </p>

      {/* Retry button */}
      {onRetry && (
        <button
          onClick={onRetry}
          className="rounded-lg bg-[#111111] px-4 py-2 text-sm font-bold text-[#F5F2D8] hover:bg-[#1A1A1A] transition-colors duration-150"
        >
          Muat Ulang
        </button>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | grep -E "pdf-viewer-skeleton|pdf-viewer-error|error TS" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "animate-pulse" frontend/components/workspace-v2/pdf-viewer-skeleton.tsx` → ditemukan minimal 3 kali (multiple shimmer bars)
    - `grep -n "aria-busy=\"true\"" frontend/components/workspace-v2/pdf-viewer-skeleton.tsx` → ditemukan
    - `grep -n "Memuat dokumen" frontend/components/workspace-v2/pdf-viewer-skeleton.tsx` → ditemukan (copy Indonesian)
    - `grep -n 'role="alert"' frontend/components/workspace-v2/pdf-viewer-error.tsx` → ditemukan
    - `grep -n "Dokumen Tidak Tersedia" frontend/components/workspace-v2/pdf-viewer-error.tsx` → ditemukan (heading teks)
    - `grep -n "Muat Ulang" frontend/components/workspace-v2/pdf-viewer-error.tsx` → ditemukan (tombol retry)
    - `grep -n "AlertTriangle" frontend/components/workspace-v2/pdf-viewer-error.tsx` → ditemukan (icon dari lucide)
    - `grep -n "PDF tidak tersedia" frontend/components/workspace-v2/pdf-viewer-error.tsx` → ditemukan (error message no-file)
    - `grep -n "bg-\[#FFFDF4\]" frontend/components/workspace-v2/pdf-viewer-skeleton.tsx` → ditemukan (paper color)
    - `npx tsc --noEmit` → exit 0 untuk semua workspace-v2 components
  </acceptance_criteria>
  <done>
    PdfViewerSkeleton dengan shimmer animate-pulse bars dalam paper shape. PdfViewerError dengan role="alert", AlertTriangle icon, heading "Dokumen Tidak Tersedia", 3 error message variants, tombol "Muat Ulang". TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Presigned URL → react-pdf Document | URL datang dari backend presigned R2 — CORS harus dikonfigurasi di R2 bucket untuk allow GET dari frontend origin |
| pdf.worker.min.mjs | Binary worker di /public/ — tidak ada user input path; disajikan sebagai static asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | PdfViewerError messages | accept | Error messages tidak mengekspos detail sistem; hanya user-friendly Indonesian copy |
| T-13-04-02 | Denial of Service | ResizeObserver | mitigate | `observer.disconnect()` di cleanup useEffect — tidak ada memory leak |
| T-13-04-03 | Tampering | pdf.worker.min.mjs di /public/ | accept | File statis dari npm registry; tidak dapat dimodifikasi tanpa akses filesystem; Next.js serves as static |
| T-13-04-04 | Information Disclosure | CORS — R2 presigned URL | mitigate | RESEARCH.md mencatat R2 butuh CORS policy. Jika tidak dikonfigurasi, browser block request — hanya PdfViewerError ditampilkan, tidak ada data leak |
</threat_model>

<verification>
```bash
# Verifikasi semua 5 file component ada
ls frontend/components/workspace-v2/pdf-viewer*.tsx

# Verifikasi worker file ada
ls frontend/public/pdf.worker.min.mjs

# TypeScript check
cd frontend && npx tsc --noEmit
```

```bash
# Verifikasi key patterns
grep -n "renderTextLayer={true}" frontend/components/workspace-v2/pdf-viewer-inner.tsx
grep -n "ssr: false" frontend/components/workspace-v2/pdf-viewer.tsx
grep -n "ResizeObserver" frontend/components/workspace-v2/pdf-viewer-panel.tsx
grep -n 'role="alert"' frontend/components/workspace-v2/pdf-viewer-error.tsx
```
Semua harus ditemukan.
</verification>

<success_criteria>
- pdf-viewer-inner.tsx: react-pdf Document+Page, renderTextLayer=true, worker path statis "/pdf.worker.min.mjs"
- pdf-viewer.tsx: dynamic() dengan ssr:false, menampilkan PdfViewerSkeleton saat loading
- pdf-viewer-panel.tsx: ResizeObserver mengukur width, paper styling (#FFFDF4, shadow, rounded-2xl)
- pdf-viewer-skeleton.tsx: shimmer bars animate-pulse, aria-busy=true, copy "Memuat dokumen..."
- pdf-viewer-error.tsx: role=alert, heading "Dokumen Tidak Tersedia", tombol "Muat Ulang"
- frontend/public/pdf.worker.min.mjs ada
- TypeScript clean (tsc --noEmit exit 0)
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/13-pdf-first-workspace-shell/13-04-SUMMARY.md` dengan:
- Konfirmasi worker file ditemukan di path yang benar dan berhasil di-copy
- TypeScript check result (tsc --noEmit)
- Apakah ada issue dengan react-pdf imports
- List semua 5 file yang dibuat
</output>
