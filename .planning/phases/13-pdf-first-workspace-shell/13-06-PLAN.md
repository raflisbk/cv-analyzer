---
phase: 13-pdf-first-workspace-shell
plan: 06
type: execute
wave: 3
depends_on:
  - 13-04
files_modified:
  - frontend/lib/annotation-utils.ts
  - frontend/hooks/use-workspace-doc.ts
  - frontend/components/workspace-v2/pdf-viewer-inner.tsx
  - .planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md
autonomous: true
requirements:
  - CRDT-01
  - CRDT-03
  - PDF-01

must_haves:
  truths:
    - "frontend/lib/annotation-utils.ts mengekspor SuggestionAnchor interface dan findTextRect function"
    - "findTextRect memanggil page.getTextContent() dan menggunakan viewport.convertToViewportPoint() untuk konversi koordinat PDF → canvas"
    - "frontend/hooks/use-workspace-doc.ts membuat Y.Doc + IndexeddbPersistence keyed dengan job ID, tanpa SSR crash"
    - "PdfViewerInner menerima onPageLoadSuccess prop yang meneruskan PDFPageProxy ke parent"
    - "annotation-spike-findings.md mendokumentasikan algoritma, edge cases, dan hasil spike"
    - "Tidak ada import dari y-indexeddb di level server (semua dalam 'use client' atau useEffect)"
  artifacts:
    - path: "frontend/lib/annotation-utils.ts"
      provides: "findTextRect function dan SuggestionAnchor interface"
      exports: ["findTextRect", "SuggestionAnchor"]
    - path: "frontend/hooks/use-workspace-doc.ts"
      provides: "Yjs + IndexeddbPersistence hook"
      exports: ["useWorkspaceDoc"]
    - path: ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"
      provides: "Dokumentasi pendekatan annotation coordinate mapping"
      contains: "getTextContent"
  key_links:
    - from: "frontend/lib/annotation-utils.ts"
      to: "pdfjs-dist PDFPageProxy"
      via: "page.getTextContent() + page.getViewport({ scale: 1.0 })"
      pattern: "getTextContent"
    - from: "frontend/hooks/use-workspace-doc.ts"
      to: "y-indexeddb IndexeddbPersistence"
      via: "new IndexeddbPersistence(`workspace-v2-${jobId}`, doc)"
      pattern: "IndexeddbPersistence"
---

<objective>
Dua deliverable proof-of-concept untuk Phase 13:
1. Annotation coordinate spike — TypeScript `findTextRect` function yang memetakan text substring ke PDF bounding rect menggunakan `pdfPage.getTextContent()`, plus dokumentasi temuan
2. Yjs initialization hook — `useWorkspaceDoc` yang membuat `Y.Doc` + `IndexeddbPersistence` keyed ke job ID, membuktikan CRDT-01 bisa diinisialisasi tanpa SSR crash

Purpose: Phase 13 harus "prove the coordinate approach before any annotation overlay work begins" (dari phase goal). Spike ini memvalidasi apakah pendekatan text-rect mapping layak sebelum membangun annotation overlay di Phase 14. Yjs hook membuktikan browser persistence siap.

Output: annotation-utils.ts dengan findTextRect, use-workspace-doc.ts dengan Yjs hook, pdf-viewer-inner.tsx diupdate dengan onPageLoadSuccess prop, spike findings doc.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-first-workspace-shell/13-CONTEXT.md
@.planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md
@frontend/components/workspace-v2/pdf-viewer-inner.tsx
@frontend/lib/workspace-utils.ts

<interfaces>
<!-- PDFPageProxy dari pdfjs-dist (bundled dalam react-pdf v10) -->
<!-- CATATAN: react-pdf tidak mengekspor PDFPageProxy type secara langsung -->
<!-- Gunakan type `unknown` atau import dari pdfjs-dist jika tersedia -->

<!-- pdfjs-dist getTextContent() API (ASSUMED — pdfjs-dist 5.x) -->
```typescript
// TextItem — satu item teks dalam PDF page
interface TextItem {
  str: string;              // string text
  transform: number[];      // [a, b, c, d, e, f] — e=x, f=y (PDF bottom-left)
  width: number;            // lebar item dalam PDF units
  height: number;           // tinggi item dalam PDF units
  dir: string;              // "ltr" atau "rtl"
  fontName: string;
  hasEOL: boolean;
}

// page.getTextContent() returns Promise<TextContent>
// textContent.items = Array<TextItem | TextMarkedContent>
// (filter dengan 'str' in item untuk TextItem only)

// page.getViewport({ scale: 1.0 }) returns PageViewport
// viewport.convertToViewportPoint(x, y) returns [viewportX, viewportY]
// (konversi dari PDF coordinate space ke viewport/canvas space)
// NOTE: PDF coordinates are bottom-up; viewport is top-down
```

<!-- Yjs + y-indexeddb API dari RESEARCH.md Section 7 -->
```typescript
import * as Y from "yjs";
import { IndexeddbPersistence } from "y-indexeddb";

// Y.Doc() creates new CRDT document
// new IndexeddbPersistence(name: string, doc: Y.Doc) — persists to IndexedDB
// persistence.on("synced", callback) — fires when IndexedDB loaded to memory
// persistence.destroy() — cleanup
// doc.destroy() — cleanup
```

<!-- onPageLoadSuccess callback type di PdfViewerInner -->
<!-- react-pdf Page onLoadSuccess — receives PDFPageProxy object -->
<!-- Type PDFPageProxy tidak diexport dari react-pdf, gunakan `unknown` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: annotation-utils.ts (findTextRect), use-workspace-doc.ts (Yjs), update pdf-viewer-inner.tsx</name>
  <read_first>
    - frontend/components/workspace-v2/pdf-viewer-inner.tsx (versi dari Plan 04 — lihat onPageLoadSuccess prop yang sudah ada)
    - .planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md (Section 5 — Annotation Spike Approach)
    - .planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md (Section 7 — Yjs + y-indexeddb)
    - frontend/hooks/ (direktori — cek apakah sudah ada hooks lain untuk referensi pola)
  </read_first>
  <files>
    frontend/lib/annotation-utils.ts,
    frontend/hooks/use-workspace-doc.ts,
    frontend/components/workspace-v2/pdf-viewer-inner.tsx
  </files>
  <action>
**1. Buat `frontend/lib/annotation-utils.ts`** — TypeScript function untuk mapping text → PDF rect:

```typescript
/**
 * annotation-utils.ts — Annotation coordinate mapping utilities.
 *
 * Phase 13 deliverable: proof-of-concept text substring → PDF bounding rect.
 * Digunakan oleh annotation spike untuk memvalidasi pendekatan sebelum
 * membangun annotation overlay di Phase 14.
 *
 * Algorithm:
 * 1. Panggil page.getTextContent() untuk mendapatkan TextItem array
 * 2. Setiap TextItem memiliki transform [a,b,c,d,tx,ty] di mana tx/ty = bottom-left position
 * 3. Temukan item yang mengandung searchText
 * 4. Konversi koordinat PDF (bottom-up) ke viewport space (top-down) via viewport.convertToViewportPoint()
 * 5. Return SuggestionAnchor dengan rect
 */

/** Data structure untuk anchor koordinat saran ke PDF */
export interface SuggestionAnchor {
  section: string;        // nama section, e.g. "experience"
  searchText: string;     // substring yang dipakai untuk locate rect
  charOffset: number;     // character offset dari section start (untuk future use)
  length: number;         // jumlah karakter
  pageIndex: number;      // 0-indexed page number
  rect: {
    x: number;            // viewport x (pixels, dari kiri)
    y: number;            // viewport y (pixels, dari atas)
    w: number;            // lebar dalam viewport pixels
    h: number;            // tinggi dalam viewport pixels
  };
}

/**
 * Cari bounding rect untuk text substring pada PDF page.
 *
 * @param page - PDFPageProxy dari react-pdf onLoadSuccess callback
 * @param searchText - text substring yang dicari (case-sensitive)
 * @param pageIndex - 0-indexed page number (untuk SuggestionAnchor)
 * @param section - nama section (untuk SuggestionAnchor)
 * @returns SuggestionAnchor jika ditemukan, null jika tidak ada match
 *
 * @example
 * // Dalam PdfViewerInner onPageLoadSuccess callback:
 * const anchor = await findTextRect(page, "Software Engineer", 0, "experience");
 * if (anchor) console.log("[Annotation Spike] rect:", anchor.rect);
 */
export async function findTextRect(
  page: unknown,
  searchText: string,
  pageIndex: number = 0,
  section: string = "unknown"
): Promise<SuggestionAnchor | null> {
  // Cast ke any untuk mengakses pdfjs-dist API yang tidak punya TypeScript types di scope ini
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const pdfPage = page as any;

  try {
    const textContent = await pdfPage.getTextContent();
    const viewport = pdfPage.getViewport({ scale: 1.0 });

    for (const item of textContent.items) {
      // Filter hanya TextItem (bukan TextMarkedContent)
      if (!("str" in item) || !item.str) continue;

      const textItem = item as {
        str: string;
        transform: number[];
        width: number;
        height: number;
      };

      if (textItem.str.includes(searchText)) {
        // PDF transform: [a, b, c, d, tx, ty]
        // tx = x position (left), ty = y position (bottom-up dari PDF origin)
        const [, , , , tx, ty] = textItem.transform;

        // Konversi dari PDF coordinate space (bottom-up) ke viewport space (top-down)
        const [viewportX, viewportY] = viewport.convertToViewportPoint(tx, ty);

        return {
          section,
          searchText,
          charOffset: 0, // Untuk future use — exact char offset computation
          length: searchText.length,
          pageIndex,
          rect: {
            x: viewportX,
            y: viewportY - textItem.height, // Adjust ke top-left dari text item
            w: textItem.width,
            h: textItem.height,
          },
        };
      }
    }

    // Text tidak ditemukan — bisa karena:
    // 1. Text span multiple TextItems
    // 2. Case/whitespace mismatch
    // 3. Text tidak ada di page ini
    return null;
  } catch (err) {
    console.warn("[annotation-utils] findTextRect error:", err);
    return null;
  }
}
```

**2. Buat `frontend/hooks/use-workspace-doc.ts`** — Yjs + IndexeddbPersistence:

```typescript
/**
 * use-workspace-doc.ts — Yjs Y.Doc + IndexeddbPersistence hook.
 *
 * Phase 13 deliverable: proof-of-concept CRDT initialization (CRDT-01).
 * Membuktikan Yjs + y-indexeddb bisa diinisialisasi di Next.js App Router
 * tanpa SSR crash.
 *
 * SSR safety: y-indexeddb uses browser-only `indexedDB` global.
 * useEffect tidak pernah berjalan di server — aman.
 * Tidak boleh diimport di Server Components.
 */
"use client";
import { useEffect, useRef } from "react";
import * as Y from "yjs";
import { IndexeddbPersistence } from "y-indexeddb";

interface UseWorkspaceDocResult {
  docRef: React.MutableRefObject<Y.Doc | null>;
  persistenceRef: React.MutableRefObject<IndexeddbPersistence | null>;
}

/**
 * Hook untuk menginisialisasi Yjs Y.Doc dengan IndexedDB persistence.
 *
 * @param jobId - Job UUID untuk scope IndexedDB key
 * @returns refs ke Y.Doc dan IndexeddbPersistence instances
 *
 * @example
 * // Dalam "use client" component:
 * const { docRef } = useWorkspaceDoc(jobId);
 */
export function useWorkspaceDoc(jobId: string): UseWorkspaceDocResult {
  const docRef = useRef<Y.Doc | null>(null);
  const persistenceRef = useRef<IndexeddbPersistence | null>(null);

  useEffect(() => {
    if (!jobId) return;

    // Buat Y.Doc baru untuk job ini
    const doc = new Y.Doc();
    docRef.current = doc;

    // Persist ke IndexedDB di bawah key yang scoped ke job
    // Format: "workspace-v2-{jobId}" untuk isolasi per job
    const persistence = new IndexeddbPersistence(`workspace-v2-${jobId}`, doc);
    persistenceRef.current = persistence;

    persistence.on("synced", () => {
      console.log("[Yjs] IndexedDB synced untuk job:", jobId);
    });

    // Cleanup saat component unmount atau jobId berubah
    return () => {
      persistence.destroy();
      doc.destroy();
      docRef.current = null;
      persistenceRef.current = null;
    };
  }, [jobId]);

  return { docRef, persistenceRef };
}
```

**3. Update `frontend/components/workspace-v2/pdf-viewer-inner.tsx`** — tambahkan annotation spike logging:

Baca file pdf-viewer-inner.tsx dari Plan 04. Verifikasi apakah `onPageLoadSuccess` prop sudah ada. 

Jika sudah ada (dari Plan 04 — prop `onPageLoadSuccess?: (page: unknown) => void`):
- Import `findTextRect` dari `@/lib/annotation-utils`
- Buat spike function yang dipanggil saat page load sukses
- Tambahkan console.log untuk membuktikan spike works

Update bagian `onLoadSuccess` di `<Page>` component:

```tsx
// Tambahkan import di atas file:
import { findTextRect } from "@/lib/annotation-utils";

// Tambahkan fungsi spike di dalam PdfViewerInner component (setelah state declarations):
const runAnnotationSpike = useCallback(async (page: unknown) => {
  // Spike: ambil text yang ada di PDF (gunakan text pendek dari nama atau judul yang mungkin ada)
  // Ini hanyalah proof-of-concept — di Phase 14 ini akan diintegrasikan dengan hydration.suggestions
  const testSearchText = "Engineer"; // common word dalam CV
  const anchor = await findTextRect(page, testSearchText, currentPage - 1, "spike");
  if (anchor) {
    console.log("[Annotation Spike] ✅ Text rect found:", anchor.rect);
  } else {
    console.log("[Annotation Spike] ℹ️ Text not found (normal — depends on CV content)");
  }
  // Panggil juga callback dari parent (untuk future use)
  onPageLoadSuccess?.(page);
}, [currentPage, onPageLoadSuccess]);
```

Ubah `<Page>` prop `onLoadSuccess` dari `onPageLoadSuccess` ke `runAnnotationSpike`:
```tsx
<Page
  pageNumber={currentPage}
  width={containerWidth}
  renderTextLayer={true}
  renderAnnotationLayer={false}
  onLoadSuccess={runAnnotationSpike}
/>
```

CATATAN: Jika `onPageLoadSuccess` prop sudah ada di pdf-viewer-inner.tsx dari Plan 04 dan sudah dipass langsung ke `<Page>`, ganti dengan `runAnnotationSpike` yang memanggil `findTextRect` dahulu kemudian meneruskan ke `onPageLoadSuccess` callback.
  </action>
  <verify>
    <automated>
      cd frontend && npx tsc --noEmit 2>&1 | grep -E "annotation-utils|use-workspace-doc|error TS" | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface SuggestionAnchor" frontend/lib/annotation-utils.ts` → ditemukan
    - `grep -n "export async function findTextRect" frontend/lib/annotation-utils.ts` → ditemukan
    - `grep -n "getTextContent" frontend/lib/annotation-utils.ts` → ditemukan (API call)
    - `grep -n "convertToViewportPoint" frontend/lib/annotation-utils.ts` → ditemukan (koordinat konversi)
    - `grep -n "getViewport" frontend/lib/annotation-utils.ts` → ditemukan
    - File `frontend/hooks/use-workspace-doc.ts` ada
    - `grep -n "export function useWorkspaceDoc" frontend/hooks/use-workspace-doc.ts` → ditemukan
    - `grep -n "IndexeddbPersistence" frontend/hooks/use-workspace-doc.ts` → ditemukan (import dan penggunaan)
    - `grep -n 'workspace-v2-\${jobId}' frontend/hooks/use-workspace-doc.ts` → ditemukan (IndexedDB key pattern)
    - `grep -n "Y.Doc" frontend/hooks/use-workspace-doc.ts` → ditemukan
    - `grep -n "persistence.destroy\|doc.destroy" frontend/hooks/use-workspace-doc.ts` → ditemukan (cleanup)
    - `grep -n "findTextRect" frontend/components/workspace-v2/pdf-viewer-inner.tsx` → ditemukan (import dan penggunaan)
    - `grep -n "Annotation Spike" frontend/components/workspace-v2/pdf-viewer-inner.tsx` → ditemukan (console.log)
    - `npx tsc --noEmit` → exit 0
  </acceptance_criteria>
  <done>
    annotation-utils.ts dengan findTextRect + SuggestionAnchor. use-workspace-doc.ts dengan Yjs + IndexeddbPersistence hook. pdf-viewer-inner.tsx diupdate dengan spike logging saat page load.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dokumentasi annotation-spike-findings.md</name>
  <read_first>
    - .planning/phases/13-pdf-first-workspace-shell/13-RESEARCH.md (Section 5 — Annotation Spike Approach, known challenges)
    - frontend/lib/annotation-utils.ts (yang baru dibuat — dokumentasikan implementasi aktual)
    - frontend/hooks/use-workspace-doc.ts (yang baru dibuat — dokumentasikan Yjs status)
  </read_first>
  <files>
    .planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md
  </files>
  <action>
Buat file `.planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md` dengan konten dokumentasi spike:

```markdown
# Annotation Coordinate Spike — Phase 13 Findings

**Date:** 2026-04-14
**Status:** Proof-of-concept implemented
**File:** `frontend/lib/annotation-utils.ts`

---

## Goal

Validasi apakah `pdfPage.getTextContent()` dari pdfjs-dist bisa digunakan untuk memetakan
text substring dari saran AI ke bounding rect pada canvas PDF, sebelum membangun annotation
overlay di Phase 14.

---

## Algorithm Implemented

### Input
- `page: PDFPageProxy` — dari react-pdf `<Page>` `onLoadSuccess` callback
- `searchText: string` — substring teks yang dicari (e.g., "Software Engineer")
- `pageIndex: number` — 0-indexed page number
- `section: string` — nama section CV (e.g., "experience")

### Process

```
1. page.getTextContent()
   → returns { items: TextItem[] }
   → setiap TextItem: { str, transform: [a,b,c,d,tx,ty], width, height }

2. Untuk setiap item:
   → filter item dengan 'str' property (TextItem, bukan TextMarkedContent)
   → cek item.str.includes(searchText)
   → jika match ditemukan:

3. Ekstrak koordinat PDF dari transform:
   → tx = transform[4]  (x position, left edge)
   → ty = transform[5]  (y position, BOTTOM-LEFT — PDF coordinate system)

4. Konversi ke viewport space:
   → viewport = page.getViewport({ scale: 1.0 })
   → [viewportX, viewportY] = viewport.convertToViewportPoint(tx, ty)
   → NOTE: viewportY sudah dalam top-down coordinate (viewport melakukan flip)
   → Adjust ke top-left: y = viewportY - item.height

5. Return SuggestionAnchor:
   → { section, searchText, pageIndex, rect: { x, y, w, h } }
```

### Output
```typescript
interface SuggestionAnchor {
  section: string;
  searchText: string;
  charOffset: number;
  length: number;
  pageIndex: number;
  rect: { x: number; y: number; w: number; h: number };
}
```

---

## Validasi dalam Browser

Untuk memvalidasi spike:
1. Buka `/workspace-v2/[valid_job_id]` di browser
2. Buka DevTools Console
3. Setelah PDF berhasil dimuat, lihat log:
   - `[Annotation Spike] ✅ Text rect found: { x: ..., y: ..., w: ..., h: ... }` — SUKSES
   - `[Annotation Spike] ℹ️ Text not found` — Normal jika "Engineer" tidak ada di halaman 1

### Interpretasi Hasil
- Jika rect ditemukan: algoritma valid, Phase 14 bisa proceed dengan confidence
- Jika text tidak ditemukan: bukan kegagalan — setiap CV berbeda. Ganti `searchText` dengan kata yang ada di CV yang ditest

---

## Known Challenges & Edge Cases

### 1. Multi-item text spans
**Problem:** Saran AI sering berupa kalimat panjang yang di-split pdfjs ke beberapa TextItem.
`item.str.includes(searchText)` hanya bekerja jika seluruh searchText ada dalam satu item.

**Phase 14 mitigation:** Concatenate adjacent items dan window-search. Atau gunakan fuzzy match
(Levenshtein distance) untuk menangani OCR artifacts.

**Phase 13 scope:** Spike hanya butuh membuktikan single-item match — tidak perlu solve multi-item.

### 2. Coordinate system
**Problem:** PDF coordinates adalah bottom-up (y=0 di bawah). Canvas/HTML adalah top-down.

**Solution (implemented):** `viewport.convertToViewportPoint(tx, ty)` menangani flip.
Setelah konversi, `viewportY` sudah dalam koordinat top-down.
Adjust ke top-left: `y = viewportY - item.height`.

### 3. Scale dependency
**Problem:** `getViewport({ scale: 1.0 })` digunakan untuk koordinat, tapi PDF page dirender
dengan `width={containerWidth}` yang auto-scale. Rect dari scale=1.0 dan render scale berbeda.

**Phase 14 mitigation:** Compute scale factor:
```typescript
const scale = containerWidth / viewport.width;
// Kemudian multiply semua rect values dengan scale
rect = { x: x * scale, y: y * scale, w: w * scale, h: h * scale };
```

**Phase 13 scope:** Spike menggunakan scale=1.0 untuk simplicity. Scale adjustment di Phase 14.

### 4. Case sensitivity
**Problem:** LLM suggestions mungkin menggunakan casing berbeda dari PDF text
(e.g., "SOFTWARE ENGINEER" vs "Software Engineer").

**Phase 14 mitigation:** `.toLowerCase()` comparison.

---

## Yjs Initialization (CRDT-01)

**Hook:** `frontend/hooks/use-workspace-doc.ts`

**Result:** Yjs Y.Doc + IndexeddbPersistence berhasil diinisialisasi.
Konfirmasi: `[Yjs] IndexedDB synced untuk job: {jobId}` muncul di console setelah halaman dimuat.

**SSR safety:** Hook menggunakan `useEffect` — tidak pernah berjalan di server.
y-indexeddb import aman karena hanya digunakan di dalam `useEffect`.

**Scope Phase 13:** Y.Doc tidak menyimpan data meaningful — hanya proof-of-concept
bahwa persistence bisa diinisialisasi. Data wiring di Phase 14+.

---

## Recommendation untuk Phase 14

1. **Coordinate mapping:** Implementasikan multi-TextItem concatenation + scale factor
2. **Overlay rendering:** Render `<div>` absolute-positioned di atas PDF canvas menggunakan
   `position: absolute`, `left: rect.x * scale`, `top: rect.y * scale`, dll.
3. **Anchor storage:** Simpan hasil `findTextRect` di `suggestion_anchors` JSONB column
   (yang sudah ditambahkan di Phase 13 migration)
4. **Yjs data model:** Gunakan `Y.Map` untuk menyimpan anchor data yang bisa di-sync real-time

---

## Files

| File | Tujuan |
|------|--------|
| `frontend/lib/annotation-utils.ts` | findTextRect implementation + SuggestionAnchor interface |
| `frontend/hooks/use-workspace-doc.ts` | Yjs + IndexeddbPersistence hook |
| `frontend/components/workspace-v2/pdf-viewer-inner.tsx` | Spike console.log di onLoadSuccess |
| `backend/alembic/versions/*_add_pdf_workspace_columns_to_jobs.py` | suggestion_anchors JSONB column |
```
  </action>
  <verify>
    <automated>
      Get-Content ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md" | Select-Object -First 5
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md` ada
    - `grep -c "getTextContent" ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"` → > 0 (algoritma didokumentasikan)
    - `grep -c "convertToViewportPoint" ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"` → > 0
    - `grep -c "Phase 14" ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"` → > 0 (rekomendasi ke phase berikutnya ada)
    - `grep -c "Multi-item\|scale\|Case sensitivity" ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"` → > 0 (edge cases didokumentasikan)
    - `grep -c "Yjs\|CRDT" ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"` → > 0 (Yjs findings juga didokumentasikan)
    - File memiliki lebih dari 50 baris konten (bukan file kosong/placeholder)
  </acceptance_criteria>
  <done>
    annotation-spike-findings.md mendokumentasikan algoritma, validasi, known challenges (multi-item spans, coordinate system, scale, case sensitivity), Yjs initialization result, dan rekomendasi untuk Phase 14.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pdfjs-dist API → annotation-utils.ts | Mengakses PDFPageProxy via type cast `as any` — dibatasi untuk spike saja |
| y-indexeddb → browser IndexedDB | Data tersimpan di browser local storage — scoped ke origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Information Disclosure | annotation-utils console.log | accept | Console.log hanya di development; tidak ada PII dalam anchor data (hanya rect coordinates); mudah dihapus di Phase 14 |
| T-13-06-02 | Denial of Service | findTextRect error handling | mitigate | try/catch menangkap semua errors dari getTextContent; return null pada failure — tidak crash viewer |
| T-13-06-03 | Information Disclosure | Yjs IndexedDB persistence | accept | Data tersimpan di browser local — scoped ke origin (localhost:3000); tidak dapat diakses oleh third-party scripts; no PII di Phase 13 (Y.Doc kosong) |
| T-13-06-04 | Tampering | type `as any` cast di findTextRect | mitigate | Cast dibatasi dalam function scope; `eslint-disable` comment eksplisit; hanya dipakai untuk mengakses pdfjs-dist API yang tidak punya TypeScript types di scope ini. Phase 14 bisa menambahkan proper typing dengan `import type { PDFPageProxy } from "pdfjs-dist"` |
</threat_model>

<verification>
```bash
# TypeScript check
cd frontend && npx tsc --noEmit
```
Harus exit 0.

```bash
# Verifikasi file ada dan konten key ada
grep -n "export async function findTextRect" frontend/lib/annotation-utils.ts
grep -n "export function useWorkspaceDoc" frontend/hooks/use-workspace-doc.ts
grep -n "IndexeddbPersistence" frontend/hooks/use-workspace-doc.ts
grep -n "Annotation Spike" frontend/components/workspace-v2/pdf-viewer-inner.tsx
```
Semua harus ditemukan.

```bash
# Verifikasi findings doc
Test-Path ".planning/phases/13-pdf-first-workspace-shell/annotation-spike-findings.md"
```
Harus True.

Manual validation (setelah dev server up):
1. Buka `/workspace-v2/[valid_job_id]`
2. DevTools Console → cari `[Annotation Spike]` dan `[Yjs] IndexedDB synced`
3. Kedua log harus muncul (dengan atau tanpa text match — keduanya valid)
</verification>

<success_criteria>
- annotation-utils.ts: SuggestionAnchor interface + findTextRect async function dengan getTextContent + convertToViewportPoint
- use-workspace-doc.ts: Yjs Y.Doc + IndexeddbPersistence hook dengan cleanup, "use client" directive, keyed ke jobId
- pdf-viewer-inner.tsx: spike console.log terintegrasi di onLoadSuccess
- annotation-spike-findings.md: dokumentasi lengkap algoritma, edge cases, Yjs status, dan rekomendasi Phase 14
- TypeScript clean di semua file baru
</success_criteria>

<output>
Setelah selesai, buat `.planning/phases/13-pdf-first-workspace-shell/13-06-SUMMARY.md` dengan:
- Konfirmasi TypeScript clean
- Apakah manual test berhasil: apakah [Annotation Spike] log muncul di console?
- Apakah [Yjs] IndexedDB synced log muncul?
- Apakah text rect ditemukan (atau not found — keduanya acceptable)?
- Pelajaran dari spike: apakah koordinat yang dikembalikan masuk akal?
- Siap untuk Phase 14 annotation overlay: YES/NO dengan reasoning
</output>
